# Stage 3 Phase A — controller rulings

Every decision taken on Matt's behalf while executing tickets 33-40 subagent-driven on
2026-08-19, lifted out of the git-ignored SDD ledger so it survives the worktree. Each says
what was decided, why, and what it costs if wrong. Read alongside each ticket's `## Comments`.

37 rulings.

- **Ruling 1** — `op(on:owner:status:deviceType:quantity:)` keeps the `deviceType`/`quantity` parameters Task 1 never passes — they are Task 2's, and rewriting the helper's signature one task later would churn the file. — Why: the plan hands both tasks the same private fixture deliberately, and a reviewer's YAGNI reflex here would cost Task 2 a rewrite. — Cost if wrong: two unused default-valued parameters sit in a test file for the length of one ticket.

- **Ruling 2** — the plan's Task 1 code block is authoritative over ticket 33's prose enumeration of the six cases. — Why: the ticket points at the plan ("Plan: plan-stage-3.md — Task 1") and the plan carries the literal test bodies; both say six. — Cost if wrong: the suite covers `activeJobIsTheLiveOp` instead of splitting capability from code order, which is a naming difference, not a coverage gap — `benchesAreCapableDevicesInCodeOrder` asserts both.

- **Ruling 3** — the three 4-line `///` blocks stand as the plan wrote them. — Why: the enforced lint (`app/scripts/check-comments.sh`) tests only history-recording regexes and device codes, never line counts; and `DirectiveEngine/Sources` already carries 195 `///` blocks longer than three lines across 28 files (`RelayRun.swift` 25, `DirectiveEngine.swift` 14, `SalvageRun.swift` 15), so the neighbouring convention the same constraint section tells this file to match is emphatically 4+. Trimming three blocks in the one new file would make it the odd file out and buy nothing measurable. — Cost if wrong: three doc comments carry one line more than the written budget, fixable in a one-line edit at any later point.

- **Ruling 4 — REVERSES Ruling 3** — the three doc comments must be trimmed to 3 lines; the reviewer was right and Ruling 3 was wrong. — Why: the reviewer surfaced `app/CLAUDE.md:41-49`, which I had not read. It says the budget is "hard, not advisory" and, decisively, "Blank `///` lines count" — the policy anticipated exactly this shape and ruled against it. Ruling 3 rested on 195 legacy over-budget blocks, but the test that matters is whether code written AFTER the 2026-08-05 policy obeys it: all nine files Stage 2 added on 2026-08-17/18 (`Steps/BotPhase.swift`, `ConfirmRow.swift`, `PrintJob.swift`, `PrintRail.swift`, `ReturnHome.swift`, `StepContext.swift`, `StepResult.swift`, `StowOrAttach.swift`, `TravelTo.swift`) carry ZERO over-budget blocks. The 195 are pre-policy. The convention this file must match is Stage 2's, not the module's legacy mean. **The general lesson: a legacy violation count is not evidence a rule is dead — check what code written after the policy does.** — Cost if wrong: three doc comments lose a blank separator and read as one paragraph, which is what every Stage 2 file already does.

- **Ruling 5** — no separate final whole-branch review for this session. — Why: the session's scope is one task, and the branch diff IS the task diff, which has already had a full task review (spec + quality, sonnet) plus a scoped re-review of the fix. A third pass over the same 221 lines would re-read what two reviewers already verdicted. — Cost if wrong: the branch carries one ticket's worth of unreviewed-at-branch-level risk, recoverable by pointing the Task 2 session's final review at b7228f1..HEAD instead of at its own base.

- **Ruling 6** — the branch is NOT merged to local `main`. — Why: a merge is a side effect outside this worktree that norms say to ask about, and this job's instructions forbid merging outright. Matt merges, or tells me to. — Cost if wrong: `main` lacks ticket 33 until he says the word; the work is committed and safe on `worktree-directives-stage-3`.

- **Ruling 7** — implementers for tasks 4-8 locate their edit sites **by symbol, not by the line numbers their briefs cite**. — Why: the briefs were pinned against `97585e1`, and tasks 3, 6 and 7 edit the same two files in sequence, so every later citation drifts by the size of the earlier edit. — Cost if wrong: an implementer edits the wrong block, which the task review catches against the brief's described intent.

- **Ruling 8** — Task 6 calls the **one-argument** `desiredIdle(for:)` and does not compute a bench count; Task 7 introduces `desiredIdle(for:benches:)` and updates that call site. — Why: T6's Interfaces section says "Produces: nothing new" and T7's says it produces the two-argument form, so the prose is consistent with both tasks' declared scope and the code block is the outlier. — Cost if wrong: one call site is touched twice instead of once, and Task 7's diff grows by a line.

- **Ruling 9** — Checkpoint E does not block tasks 7 and 8. — Why: it is an overnight live-app observation I cannot run, and its own stated gate is "Phase B must not start" — it gates Phase B, not the rest of Phase A. Parking the session on it would cost a day and deliver nothing. — Cost if wrong: tasks 7-8 are built on an unconfirmed premise; both are independently tested, and Matt can still run Checkpoint E before Phase B.

- **Ruling 10** — `RestockRunTests.swift` **does not exist** — tasks 6 and 7 cite it as an edit target. Create it, and put the new RestockRun cases there; leave the existing `idleCap` / `desiredIdle` cases in `RelayReturnAndRestockTests.swift:377-394` where they already live and edit them in place. — Why: the C-table names C2's and C8's tests as living in `RestockRunTests`, and both briefs list the two files separately, so the plan assumed a file that was never written. — Cost if wrong: RestockRun cases sit in two files instead of one, which is already true of this module.

- **Ruling 11** — `EventRunPrintTests.swift` **does not exist** — Task 4 cites it and half-anticipates this ("or wherever the event print cases live"). Create it for C3 and C4. Existing EventRun printing-step cases live in `EventRunTests.swift`, `EventRunEngineTests.swift` and `EventRunOptionResolutionTests.swift` — read before writing, edit in place if a case there asserts the old behaviour. — Cost if wrong: same as Ruling 10.

- **Ruling 12 (confirms the brief's own instruction)** — `RelayRun.hub(near:in:)` has exactly ONE caller, `RelayRun.swift:321` inside `acquire`, which Task 5 rewrites. Deletion is safe. Found by grep over `app/Modules` excluding `.build`, not by LSP.

- **Ruling 13 — a plan defect, ruled and fixed before review. The brief's Step 5 predicts that** — dropping `choose`'s `$0.activeJob == nil` conjunct reddens `allBusyYieldsNil`. **It does not**, and I confirmed it by reading the fixture rather than taking the report's word: that test gives B1 and B2 a `printing:` block each, so `queueDepth == 1` for both and the `queueDepth == 0` conjunct alone already excludes them. The `activeJob` guard therefore has NO defender — exactly the Stage 2 failure the Global Constraints' mutation rule exists to catch, and the constraint says in terms that a green suite under the mutation means the test was written relative to the implementation. — Decision: add ONE test isolating the guard — a bench whose ops table carries an open print while its device snapshot is still empty (`queueDepth == 0`, `activeJob != nil`). That state is reachable and is the point of C11: `openOperation(for:)` reads the local ops table, `queueDepth` reads the device `detail` blob, so a print dispatched seconds ago has a row before the device poll returns a `printing` block. Without the guard a mission double-dispatches onto that bench and the supersede eats the co-tenant's op. — Cost if wrong: a seventh test in the choice suite asserting a state that turns out unreachable, deletable in one edit.

- **Ruling 14** — Task 3's implementer is dispatched while Task 2's review is still running. — Why: the skill forbids two IMPLEMENTERS in parallel, not an implementer alongside a reviewer, and six tasks remain; the only coupling is `choose`'s signature, which Task 3 is told to read off disk rather than trust from its dispatch. — Cost if wrong: if the review changes `choose`'s signature, Task 3 takes a fix round it would not otherwise have needed.

- **Ruling 15** — I2 is upheld, not parked. — Why: beyond the policy, the comment asserts "Today `PrintJob.bench` falls back to a busy bench", and Task 3 — running right now — is the task that makes that false. A comment that describes another function's behaviour goes stale the moment that function changes, which is the case here within the hour. Replace it with the invariant the test actually pins: no free bench means no choice, and the caller waits. — Cost if wrong: one test doc comment loses a cross-reference to C11 that the plan and the ticket both still record.

- **Ruling 16** — Task 2's fix round waits for Task 3's implementer to finish rather than running concurrently. — Why: two writing agents share one `.build` and one index; a full-suite run launched while the other has a source file half-edited fails for reasons that belong to neither, and `git add -A` from either would stage the other's work. A read-only reviewer alongside a writer (Ruling 14) is safe; two writers are not. — Cost if wrong: Task 2's fix starts a few minutes later than it could have.

- **Ruling 17** — I3 is named and kept, not reverted. — Why: the reviewer is right that it is a behaviour change and right that it should have been declared, but it is forced — once `bench` takes a depot designation rather than a device, there is no device to scope the old check to, and the device-scoped question was already the wrong one: substitution means the bench holding our job is often not the bench we asked about, which is exactly what `PrintJob.stillPrinting` exists to answer (`PrintJob.swift:49-51` says so). Reverting it would reintroduce the bug Stage 2 wrote `stillPrinting` to fix. It is mutation-probed. — Action: it gets a named line in the ticket's `## Comments` and in the plan's deliberate-behaviour-change list as **C12**, so it is not folded silently into C11. — Cost if wrong: a behaviour change ships named but without a plan-level sign-off, visible in the ticket.

- **Ruling 18 — resolving the reviewer's ⚠️, which it could not settle from the diff. The concern** — is real: Task 3 removed the own-op guards that were the only thing stopping a mission re-dispatching while its OWN earlier print is open on a different bench at the same depot, and `PrintScheduler.choose` never reads `order.owner`, so it cannot know. I checked the plan rather than the diff. **Task 6 (ticket 38) is what closes it** — C1 is precisely "orders against remaining demand netted by `onOrder`", and `onOrder(for:at:in:)` counts the owner's own open print ops. So the guard is not deleted, it is replaced, three tasks later. The window between

- **Ruling 19 — C4 IS WITHDRAWN AS SPECIFIED. The plan's C4 says "`EventRun`'s bench-busy guard** — becomes owner-scoped", pinned by "a co-tenant's print does not hide a bench". I verified the implementer's claim against the old source rather than taking it on trust: - old guard, `EventRun.swift:411` at a358fdf: `world.openOperation(for: $0.deviceCode) == nil` — ANY-owner, per-device. - new guard, `PrintScheduler.choose`: `$0.activeJob == nil`, where `activeJob` is `world.openOperation(for: device.deviceCode)` — ANY-owner, per-device. They are the same predicate, so nothing about owner-scoping changes and the C4 test passes vacuously either way. Worse, **C4 as written contradicts C11**: C11 deliberately stops a mission dispatching onto a bench busy with another run's print, and C4 asks for a co-tenant's print NOT to hide a bench. Those are opposites. C11 is the later change, carries a named live bug (fallback-to-busy-bench, then `CommandClient` supersedes the co-tenant's op), and is already implemented across four sites. **C11 wins; C4 is struck.** — Cost if wrong: EventRun keeps a bench-busy rule identical to the one it had, which is also the rule every other print site now follows.

- **Ruling 20 — C4 is REPLACED by C4', the change this migration actually makes. `printsInFlight`** — (`EventRun.swift:275-285` at a358fdf) iterates `world.dispatchedOperations.values` with **no location filter**, so a print this directive has open at ANOTHER depot nets against demand here and suppresses an order that should go out. `PrintScheduler.onOrder(for:at:in:)` is depot scoped, so replacing one with the other closes a real cross-depot leak. Pinned by the implementer's own new test `anotherDepotsPrintDoesNotNetHere`, which it added after finding the brief's C4 test vacuous. — C4' reads: *`EventRun`'s on-order accounting becomes depot scoped; a print open at another depot no longer suppresses an order here.* — Cost if wrong: the ticket names a behaviour change that was really a bug fix, which is a labelling error.

- **Ruling 21** — Task 4 keeping the `PrintJob.hasBench` stall-guard that the brief's Step 3 snippet silently drops is CORRECT and needs no change. — Why: dropping it turns a zero-bench depot from `.stall(.unreachableDevice)` into `.wait`, an unnamed behaviour change, and it would have made EventRun the one site of five not following Task 3's stall/wait split. — Cost if wrong: none identified; it is mutation-probed and pinned by `noBenchesAtAllStalls`.

- **Ruling 22** — I2 — the test is KEPT, renamed and re-documented, not dropped. — Why: read for what it actually constructs rather than what it is called, it pins something real and worth keeping: a co-tenant's print occupies its own bench without blocking dispatch at the depot, which is the fan-out property the whole of Phase A exists to deliver. Dropping it would lose that coverage to fix a naming error. — Cost if wrong: one test carries a name a future reader has to match against its body, which is the situation today anyway.

- **Ruling 23** — the reviewer's Minor 1 is PROMOTED into the fix. `anotherDepotsPrintDoesNotNetHere` is the pinning test for C4', this task's headline change, but its "ELSEWHERE" op names an entity that is never registered as a `Device`, so it proves "an op on a non-bench entity does not net" rather than "an op at another depot does not net". It exercises the right conjunct by accident. — Why promote a Minor: a mis-modelled fixture on the one test defending this task's real behaviour change is the same class of defect as the vacuous C4 test this task was praised for catching. Adding a bench at a second depot is a two-line fixture change. — Cost if wrong: two lines of fixture.

- **Ruling 24 — the comment-lint baseline, established so later reviews do not bill this branch for** — inherited debt. I ran `check-comments.sh` myself over both trees: - `DirectiveEngine/Sources`: ONE hit, `DirectiveEngine.swift:532`. Verified byte-identical at the branch base b7228f1, so it predates this work. - `DirectiveEngine/Tests`: 62 hits, none of them in a file this branch created — `PrintSchedulerTests.swift` and `EventRunPrintTests.swift` are clean. Eight are in `RelayRunTests.swift`, which Task 5 is editing right now. The line: a pre-existing violation in a file a task merely edits is NOT that task's to fix.

- **Ruling 25** — I1 is upheld and is the round's blocking item. The implementer replaced a vacuous brief test with a second test that passes for the wrong reason — the fifth instance of this failure on the branch, and the first where the replacement was the problem. The fix must populate `dispatchedOperations` for the owner and use a carrier code distinct from every bench, so the owner-scoped `mine` path is what does the work. — Cost if wrong: C7 ships pinned by a fixture coincidence, and the guard protecting RelayRun from double-ordering has no defender.

- **Ruling 26** — the read drop is named **C13** — *the fleet-evidence re-read is skipped whenever the reserve rail already vetoes, because the gate now sits after the rail rather than before it.* Not C12; that is Task 3's `RestockRun.printing` change. — Cost if wrong: a named row that is really a performance consequence rather than a policy change.

- **Ruling 27** — I3 is fixed NOW, not deferred to Task 15 as the reviewer offered. — Why: "five sites, one idiom" is the stated purpose of this stage, and `RelayRun` is the fifth site; deferring means

- **Ruling 28** — the three Minor doc comments are PROMOTED into the fix. — Why: they narrate a function this same diff deletes, so they are not merely stylistic — they point at code that no longer exists. Identical defects were fixed in tasks 3 and 4; leaving these means the branch ships two standards. — Cost if wrong: three comments rewritten that nobody would have read.

- **Ruling 29 — a REAL DEFECT in the plan's own acceptance-task code, found by the implementer and** — confirmed by me by reading the source rather than the report. `MineFleetPrint.stocking` as the brief specifies it computes the true shortfall from device rows, subtracts what is already on order, and then returns `.done` when the REMAINDER is empty:

- **Ruling 30** — I1 is fixed by REWRITING the test to drive through `printing`, not by deleting the dead assertion as the reviewer offered. — Why: the reviewer established that the deadline logic lives only in `printing`, and this task rewrote `printing` into a thin deadline holder. If the vacuous assertion is simply deleted, that rewritten step's deadline transition has no test at all — trading a false defender for no defender. — Cost if wrong: one test costs more to write than a deletion would.

- **Ruling 31** — the Minor "Ruling 3" doc labels are PROMOTED into the fix. — Why: same class as I2 — a label naming a controller decision from a workspace that dies with this worktree is a pointer to nothing for any future reader. Two short edits in files already being touched.

- **Ruling 32** — I1 is fixed by ADDING a test that defends the gate's position, and the vacuous one goes. — Why: same shape as Ruling 30. Deleting `metDemandBuysNoSweep` alone leaves the sweep's placement — the entire point of automation-brain ticket 14, which is about buying evidence BEFORE the spend and after every cheaper decline — with no defender. The test to write: an earlier declining branch fires (rail short, or census stale) WHILE fleet evidence is also stale, and the expected action is the earlier branch's, not the sweep. That is what proves the sweep sits last. — Cost if wrong: one more test in a suite this task already owns.

- **Ruling 33** — the Minor file-header finding on `BrainGrowLifecycleE2ETests.swift` is PARKED, not fixed. — Why: my own carve-out (Ruling 24) does make it this task's, since the task rewrote a sentence inside the block. But the block was 16 lines before this branch existed and this task did not lengthen it; trimming it to 10 means rewriting inherited content in a file this task touched only to add a stub. That is scope creep dressed as compliance. — Cost if wrong: one pre-existing over-long file header survives in a file this branch edited.

- **Ruling 34** — the Minor "Ticket 18's formula." provenance pointer at `RestockRunTests.swift:119` is PROMOTED into the fix. — Why: third time this class has appeared — the C4 narration, the "Ruling 3" labels — and I promoted it both previous times. A pointer to a ticket number without naming where the contract lives is a dead reference to a future reader. Consistency costs one line here. — Cost if wrong: one comment rewritten.

- **Ruling 35** — I1 is fixed by MOVING the line into `activeJob(_:)` beside the "Printing" row, not by rewording it in place. — Why: the sentence is already accurate; what misleads is its adjacency to a list of unattributable rows. The List view proves the right shape — there the claim sits beside the thing it describes and the reviewer found no problem with it. Moving it also dissolves the Minor wording concern, since "Ordered by" is only ambiguous with "sequenced by" when it sits above a numbered list. — Cost if wrong: the owner line appears in a different section of the detail view than the plan drew it.

- **Ruling 36** — the Minor untested sort tie-break at `PrintQueueOwners.swift:22` is FIXED, not parked, against the reviewer's own rating. — Why: the reviewer rated it cosmetic because two open prints on one bench at the same instant cannot happen under Phase A's `operation_one_open_per_device` index. **Phase B ticket 42 removes that index**, and ticket 46 makes `owners` hold N elements by design — so the comparator becomes reachable in the very next phase, and it is undefended today. A fixture with two equal `startedAt` values costs two lines now and would cost a debugging session later. — Cost if wrong: one test defending a comparator that Phase B never gets to.

