# Stage 3 Phase B — controller rulings

Every decision taken on Matt's behalf while executing tickets 41-47 subagent-driven on
2026-08-19, lifted out of the git-ignored SDD ledger so it survives the worktree. Each says
what was decided, why, and what it costs if wrong. Phase A's are in
`stage-3-phase-a-rulings.md`; read both alongside each ticket's `## Comments`.

**B1 was wrong** — it put the migration at #55 when the manifest held 49 and it is #50. It is
kept here with the correction that follows it rather than quietly deleted, because a wrong
number asserted confidently is exactly what a later reader takes on trust.

- **Ruling B1** — **the index-relax migration is #55, not #48.** — Why: the manifest at `GameDatabase.swift:47` now holds 54 entries; `main` appended `HaulYield.addCollectedAtIndex` and `DirectiveLogEntry.addDispatchLookupIndex` after the `Directive.addFreighterCodes` that memory recorded as last. Migrations are append-only and `SchemaManifestTests` freezes the identifier list, so an implementer working from the stale number would either collide or reorder. — Cost if wrong: the migration lands at the wrong index and `SchemaManifestTests` reddens immediately, which is a loud failure rather than a silent one.

- **Ruling B2** — **ticket 43's scope expands to `Reconciler.completionMayClose`**, and Matt named this himself when clearing Phase B. — Why: the plan was written when a device-type guard sat in `completeOpenOperation`, and ticket 43's job was to turn that refusal into a selection before Task 10's relax made refusal wrong. `daf33af` on main moved it: the three guards are now one `completionMayClose` (`Reconciler.swift:477`) that **both** `applyDeviceEvent` and `applyOperationEvent` ask, because the device path — the one production actually uses — never called `completeOpenOperation` at all. The three unordered `fetchOne`s over `liveCases` still exist at `:126`, `:285-286` and `:447-448`, so the brief's original three sites survive; the new work is that `completionMayClose` must select rather than refuse. — Cost if wrong: after Task 10 relaxes the index, a completion arriving against N live ops is dropped on the floor, which is the exact stall the triage of 2026-08-19 was chasing.

- **Ruling B3** — **Task 14 must NOT multiply depth by quantity.** — Why: the plan's Open Question 4 assumed a `quantity: N` job takes one queue slot and worried the formula under-counts. I settled it from the live ledger: bench `89130889` carried exactly one open print op (`service_bot`, `quantity: 2`) while its device row held one unit on the platen and **one entry in `print_queue`** with the same type and tags. A job of N occupies N slots, one at a time, so `print_queue` entries are UNITS and `queuedJobCount + (printingSnapshot != nil ? 1 : 0)` is already a true slot count. `Device.queueSize` is a ceiling in the same units. — Cost if wrong: a scheduler that double-counts every multi-unit job and refuses to queue behind benches that have room.

- **Ruling B4** — **Checkpoint F does not block Task 15.** — Why: same shape as Checkpoint E, which I ruled the same way and which Matt then ran and passed. F is an overnight live observation after Task 14; Task 15 is tidy-up, doc comments and the measurement write-up, none of which depends on what F sees. Parking the session for a day to write doc comments buys nothing. — Cost if wrong: Task 15's recorded measurement is written before F confirms it, and would need amending.

- **Ruling B5** — the Minor provenance pointer is DEFERRED TO TASK 15, not fixed in a round of its own. — Why: the task is otherwise Approved, so closing this would cost a full fix dispatch plus a scoped re-review for one cosmetic comment, and Task 15 is by definition the tidy-up ticket that edits doc comments. That is a real destination rather than a silent discard. In Phase A I promoted this class three times, but each of those was in a file already open in a fix round that was happening regardless. — Cost if wrong: one comment naming two task numbers survives until ticket 47.

  ↳ **Correction to B1.** on the number and the implementer was right. I said the migration would be #55 off an awk that counted trailing commas and so miscounted multi-line entries. Counted properly: the manifest held 49 and `GameModels.Operation.relaxOpenIndex` is **#50**, appended last, symbol at `Operation.swift:301`. The part of B1 that carried weight — append at the end, never reorder, `SchemaManifestTests` freezes the list — was right and was followed, and `GameDatabaseTests` 22/22 is the authority that settles it either way. Recorded because a wrong number I asserted confidently is exactly the kind of thing a later reader would take on trust.

- **Ruling B6** — the vacuous assertion at `CommandClientTests.swift:283` is REMOVED in the same fix round as the comment, not kept as a forward guard. — Why: an assertion that cannot fail is a false defender, which is this branch's signature defect at eleven instances and counting, and the reviewer confirmed `GameDatabaseTests` already covers the same invariant directly at the DB level. A guard that only becomes meaningful if print confirm changes is better written when it changes. — Cost if wrong: one line of forward-looking coverage removed from a suite that has the real coverage elsewhere.

- **Ruling B7** — the stale "21 migrations" comment at `DatabaseEraseResetTests.swift:43` is DEFERRED TO TASK 15, alongside Ruling B5's provenance pointer. — Why: pre-existing and untouched by this diff, so not this task's under the carve-out I set in Phase A; Task 15 is the doc-comment tidy ticket and now has two real items waiting. — Cost if wrong: a comment reading "21" against a manifest of 50 survives until ticket 47.

- **Ruling B8** — I1 is upheld and fixed. — Why: the implementer called the broadening "strictly safer" and that is a claim rather than a finding; the reviewer showed a deterministic case where it is strictly worse — a live op that should be promoted is silently left alone because an optimistic sibling bound `matchingOp` first. An argument from typical timestamp ordering is exactly the kind of unenforced assumption this effort has been burned by. — Cost if wrong: `matchingOp` scans a narrower set than the implementer judged safest, and any desire to suppress an adopt while a same-kind optimistic op is in flight has to be written as its own named, tested check.

- **Ruling B9** — the Minor at `ReconcilerSharedBenchTests.swift:451-453` is PROMOTED into the fix. The suite doc still carries a live-fleet incident figure ("23 of 235 resolved prints"), which `app/CLAUDE.md` puts in memory rather than source — and the implementer rewrote that very block, so under the carve-out I set in Phase A it is now this task's. Same class I promoted three times in Phase A. — Cost if wrong: one comment trimmed that nobody would have read.

- **Ruling B10** — the Minor at `Reconciler.swift:465-469` — `selectCompletableOp` collapsing three distinct guard-failure log messages into one — is DEFERRED TO TASK 15. — Why: it is a real loss of production diagnostic granularity, and the stall triage of 2026-08-19 was solved from exactly this kind of log line, so it should not be discarded. But it is not a correctness defect and Task 15 is the tidy ticket. Third item now waiting there, with Ruling B5's provenance pointer and Ruling B7's stale "21 migrations" comment. — Cost if wrong: one generic log line instead of three specific ones until ticket 47.

- **Ruling B11** — **the remaining Phase B order changes from 12 → 13 → 14 → 15 to 12 → 14 → 13 → 15.** Ticket 46 (Task 14) is dispatched before ticket 45 (Task 13). — Why: the branch is red and ticket 46 is precisely what closes it, by teaching the scheduler to queue on purpose instead of reading a queued bench as free. Ticket 45 is `DevicesFeature` UI and touches nothing involved, so building it first would leave three E2E failures standing for another whole ticket and make any new failure hard to attribute to its cause. No dependency is violated: Task 14 consumes `queuedOperations` (Task 9, landed) and the narrowed `openOperations` (Task 12, landed), and Task 13 consumes neither. — Cost if wrong: the plan's written sequence is departed from, and if ticket 46 turns out to need something ticket 45 produces, 46 pauses — I checked both Interfaces sections and it does not.

- **Ruling B12** — Task 12's two Minors are DEFERRED TO TASK 15. — Why: the task is Approved, so opening a fix round for them costs a dispatch plus a scoped re-review; and the skill forbids me fixing them myself, since a controller fix skips review entirely. The items: (1) the consumer-walk note for `PrintJob.stillPrinting` in ticket 44's `## Comments` says it "never reads `world.openOperations` directly", which is factually wrong — it does, transitively via `ctx.ownedOperation` -> `openOperation(for:owner:)`. The CONCLUSION is right, because `mine` (reading the status-unfiltered `dispatchedOperations`) always catches what the narrowing would hide, so it short-circuits first. But the stated reason will mislead whoever greps for it next. (2) The count clarification above should be written down where the next reader finds it. Task 15 now carries FOUR deferred items, with Ruling B5's provenance pointer, Ruling B7's stale "21 migrations" comment and Ruling B10's collapsed log messages. — Cost if wrong: a misleading sentence sits in a resolved ticket's comments until ticket 47.

- **Ruling B13** — **the failing tests are ticket 46's to resolve, not follow-up work.** The implementer called them "genuine follow-up work, not a defect of mine". I disagree, on this effort's own precedent: Task 3 carried C11 ("a mission no longer dispatches onto a bench busy with another run's print") and rewrote 8 tests asserting the old semantic, naming each with its old and new assertion; Task 6 rewrote 18 the same way. Ticket 46 changes the same semantic in the opposite direction, so the tests asserting "an occupied bench is excluded" are asserting exactly what it removes. Leaving them red is not deferral — no remaining ticket owns them, since 45 is `DevicesFeature` UI and 47 is tidy-up. — **Critical qualifier**: the instruction is NOT "make them green". For each one, decide whether it asserts the deliberately-removed semantic (rewrite, naming old and new) or whether it reveals a genuine regression (report it, leave it failing). Papering over a real regression by rewriting its test is the one outcome worse than the red. — Cost if wrong: ticket 46's diff grows by a batch of test rewrites that a later ticket would otherwise have carried, with each rewrite named and reviewable.

- **Ruling B14** — **`queueSize <= 0` must NOT mean uncapped, and this is settled by live data rather than argument.** The implementer added that reading as an evidenced judgment call beyond the brief. I queried the live database: of 13 print-capable devices, **eight report `queueSize = 0`** — `965AC2C3`, `F2908E6E`, `C7836770` and `4353FD2B` (heaven/racing vessels) and `0813351D`, `B30E7322`, `45FD581F` (structural fabricators) — against five reporting 10. Under the shipped reading, the scheduler would treat all eight as having an unbounded queue and pile work onto a heaven vessel. That is the over-print failure this entire stage exists to prevent, reintroduced one layer down. — Cost if wrong: a device that genuinely has no advertised ceiling is treated conservatively and takes fewer jobs than it could, which is the failure direction to prefer.

- **Ruling B15** — I2 is fixed rather than waved through, even though the reviewer independently confirmed both underlying test changes are sound. — Why: the classification IS the deliverable here. I told the implementer not to make the tests green but to decide, one at a time, whether each asserted a removed semantic or revealed a regression — and the table is the only record that the decision was made per test rather than in bulk. A table whose arithmetic is right while two of its rows are wrong is worse than no table, because it invites exactly the trust it has not earned. — Cost if wrong: a corrected table costs one edit and buys a record a later reader can rely on. Task 14: fix round 1/5 queued behind Task 13's implementer (one writer at a time)

- **Ruling B16** — Task 13's three Minors are DEFERRED TO TASK 15. — Why: the task is Approved, and opening a fix round costs a dispatch plus a scoped re-review for items that change no behaviour. The items: (1) `DeviceDetailView.swift:31` still fetches `order { startedAt.desc() }`, now vestigial because `DeviceOperations` re-filters and re-sorts internally; (2) no test covers an enqueued op started BEFORE the active one — genuinely low-probability now that ticket 43 promotes the oldest live op, and `card` prefers `.active` regardless of order, so it is a coverage gap rather than a risk; (3) the reviewer asked me to confirm no test other than `cardShowsTheActiveJob` was meant to read as C10 proof — **confirmed: it is the only one**, the other four prove tie-break ordering and filter correctness and are load-bearing for those. — Cost if wrong: a redundant sort and one uncovered data shape survive to ticket 47.

- **Ruling B17** — one final fix round for I1 and the two comment Minors, despite the Approved verdict. — Why: this ticket was told in terms that it "of all tickets should leave the tree cleaner than it found it" on the comment measure, and a self-review claiming a fix that did not happen is the same class as Ruling B15's classification table — the record inviting trust it has not earned. The reviewer is right that the violation is pre-existing rather than a new regression, which is why this is one tight round rather than a reopening. Folding in `CommandClient.swift`'s header, which this diff touched while correctly shrinking `Operation.swift`'s from 17 to 10 in the same commit, and the `:108`/`:109` citation. — Cost if wrong: one short round at the end of a fifteen-task plan, against a report that would otherwise overstate what it did.

