# Memory Index

- [Active replicant, not first](active-replicant-not-first.md) — multi-replicant invariant: never use roster.first / Replicant.fetchAll.first as a proxy for the active/current replicant; resolve via activeReplicantCode (count==1 fallback), reload distance sort on activeReplicantCode change, and fan event ingestion out over the whole roster when the event has no replicant_code (per-code scan debounce).

- [Same-step dispatch needs a tracked op](same-step-dispatch-needs-tracked-op.md) — a step dispatching with `nextStep` == its OWN step is safe ONLY for tracked kinds (`.travel`/`.mine`/`.print`/scan); for a `.simple` verb no Operation row exists so the `openOperation` guard is always nil, the command re-issues every tick forever, and `DirectiveExecutor` re-stamps `stepStartedAt` on each accepted dispatch so the step deadline can never fire. Split dispatch from polling (the `launching`/`awaiting` shape).
- [Confirm steps need fresh evidence](confirm-steps-need-fresh-evidence.md) — a polling step judging a LOCAL device row must first prove `updatedAt >= stepStartedAt`; nothing in a mission loop refreshes device rows, so a pre-dispatch row satisfies a loose check, the deadline never accumulates, and the run re-commands itself into a false stall — and the staleness guard must sit AFTER the deadline check or a failing read loops forever at ~12 `.high` reads/min. **Half three (2026-08-01)**: a DISPATCH step needs the same proof, keyed off the ARRIVAL not `stepStartedAt` — one arrival event closes the travel op and writes `device.location` in two separate transactions, so a tick landing between them re-commands travel at an already-parked vessel; filter the watermark op on `.completed` exactly, gate the dispatch path only, and test the gate's PLACEMENT (hoisting it above the location check broke exactly 1 of 335 tests). The `Reconciler.swift:256` same-second drop is a second, still-unfixed door to the same stall; `SurveyRun.swift:416` (`returnHome`)/`:488` (`travel`) still carry the pre-fix shape.
- [Directives feature](directives-feature.md) — automations v2: Stages 1–4 + stall resolution + the §7 step timeline SHIPPED (unified surface; CommandGovernor + DirectiveEngine; Survey Run + launcher; Retry/Skip/Cancel/Pause/Resume; one timeline query serving both row kinds). Survey Run NEVER stows or adopts — staging is the player's job. **Continuous roam shipped 2026-07-29**: `roamCentre` makes a run unbounded and self-targeting via 5-ly sliding bands; greedy nearest-neighbour was measured and rejected (leaves permanent 22-ly holes), `targets` becomes append-only history, and `.extendQueue` MUST apply to the freshly-read row or it rolls its own append back. Remaining: Stage 5 Relay Run (.opCompleted audit pass shipped 2026-07-28 — audit-only, never moves a mission).
- [SystemDetail.persist choke point](system-detail-persist-choke-point.md) — NEVER `SystemDetail.upsert` in production code; go through `SystemDetail.persist`, which also stamps `stars.fullyScannedAt` (a column nothing wrote until 2026-07-29, null on all 14,122 rows, yet read by the star map as its `.full` tier). `StarSystem.isFullyScanned` is the one completeness definition; `recon == "scanned"` is NOT a substitute (planets only, ignores moons); the stamp is write-once.
- [Brain ↔ executor seam](brain-executor-seam.md) — automation-brain ticket 04: the brain is an allocation layer above `DirectiveEngine`, touching it through create-directive / cancel-directive / driving the sanctioned `retry`/`cancel` resolution verbs — never enacting, composing, or hand-editing. Placement = a nonisolated-ranking plan loop on the existing `DirectiveEngineCore` actor (already off-main), race-free via actor + GRDB writes. Ownership = the running directive rows ARE the lease ledger (no new lease); reserve by `deviceCode`+transitive-stow / `controllerCode` / `fleetTag`; retire = status-guarded `.cancelled`. Feedback = `status`+`attentionReason` re-read each tick; HITL event choice rides `.needsAttention` as a decision-request. Stalls: mission-layer halt matrix unchanged; brain response classified by `DirectiveAttentionReason.brainDisposition` (retry/escalate/decisionRequest); auto-skip stays rejected; supply is executor self-composition. Amends `brain-robustness-bar` clause 1.
- [Brain goal & decision policy](brain-goal-decision-policy.md) — automation-brain ticket 03: the brain is a pure selector deriving goals per-tick from WorldView and allocating via one greedy lexicographic pass. Five goal kinds (survey/tendMesh[grow+prune]/mine/salvage/fulfillEvent); print/deliver/shuttle/repair are engines not goals; growFleet reserved-future. Two priority axes — acquisition order vs an inverted liveness floor protecting the enabling chain from deadlock. Spend ceiling = per-type resource reserve floor at the enactment rail + soft ≤N idle-relay cap (reclaim auto-suppresses printing). Idle keeps survey + liveness running; backoff is implicit/stateless. Vocabulary glossary now in app/CONTEXT.md.
- [Brain robustness bar](brain-robustness-bar.md) — the definition-of-done / acceptance criteria for every automation-brain capability. Spine: the brain is a PURE SELECTOR over the existing safe rails (ranks goals, never enacts; print/deliver/shuttle are new EXECUTORS, event fulfilment is a COMPOSING executor, brain is stateless between ticks). Eight checkable clauses — selector-not-enactor, stateless, pure-selection/API-vetoes-never-chooses, three-tier snapshot fidelity (staleness degrades efficiency never safety), end-to-end-through-the-seam testability, safe degradation (idle surfaced-not-escalated vs stall surfaced-and-escalated), bounded blast radius (additive writes / don't-strand / rail-enforced spend ceiling), live derived "why" view. Every capability design (automation-brain 03–06) + build plan carries a Robustness section answering each, verified in review with evidence.
- [Brain primitive contracts](brain-primitive-contracts.md) — automation-brain ticket 05: print/deliver/shuttle are ENGINES (03's sense), not goals/directives — step-libraries spliced into ONE composing `MissionStepMachine`. Key finding: they FIT the existing Directives engine, no mismatch — `deliver` is SalvageRun's shape (carrier-owned directive dispatching to OTHER devices), `print` is one step in it (`enqueue_print(device_type)` at a shared autofactory queue, NEVER leased), composing into one carrier-owned Relay Run directive (the reserved `relayRun` kind) the brain launches for `tendMesh`. Ownership (04's handed question): the carrier `deviceCode` is the ONLY lease + relay held via transitive stow → NO committed-devices field, no fleet tag. Transport gated by don't-strand (surge only to already-meshed targets; a relay MUST stow-aboard). Live-tested: pre-activating a relay then moving it (even on a surge_plate) does NOT mesh — activate in-situ, vessel present. shuttle (D4: collapse into shipped Haul Run vs distinct) deferred to ticket 06. **AMENDED by [brain-resource-hub-model]: shuttle is the RESUPPLY EXECUTOR (a generalised Haul Run), not a Relay-Run co-engine; Relay Run engine set = print+deliver only; print-in-situ rejected.**
- [Brain tendMesh worthiness](brain-tendmesh-worthiness.md) — automation-brain ticket 10 (the LAST spine decision — every capability now design-complete): the `tendMesh` grow+prune heuristic. Structural pivot — `tendMesh` is the SOLE mesh authority; relay emplacement REMOVED from the Salvage Run, no production goal plants/requests a mesh (amends map's additive preference + salvage-run-design); chain is survey (mobile, mesh-independent) → tendMesh (meshes ahead of demand) → production (derives only for meshed systems). GROW = pathfinding toward known value (salvage+mine+events; survey excluded), key `completes-unlock-now → cheapest-chain → [tier ▸ magnitude] → cost → designation`, tier = Event ▸ Rich belt ▸ Moderate belt ▸ salvage-by-units ▸ Sparse belt (belt-class pre-mesh-knowable, yields aren't); NO `critical` flag (disjoint device sets, amends 03). PRUNE = same pathfinding read inversely — a relay is USELESS iff on the cheapest anchor→live-target path-union for nothing (targets include value only reachable via a chain grow still wants) → unifies load-bearing + in-progress-hop protection + durable-uselessness, brand-new hop pinned by construction, thrash structurally impossible. Reclaim LAZY/demand-driven (source needed relay from nearest useless one before printing; retires 06's N cap); NO temporal hysteresis, sole guard = 01's confirm-before-reclaim `.high` read. Amends map/salvage-run-design + 03 + 06. **BUILT — see [brain-tendmesh-build](brain-tendmesh-build.md).**
- [tendMesh build record](brain-tendmesh-build.md) — SHIPPED 2026-08-04, the automation brain's first ACTING capability (47 commits, 27 tasks): `WorldView` + `MeshGraph` (grid adjacency + one Dijkstra serving both grow's `reach` and prune's `pathUnion`) + `ValueCatalog`/`GrowRanking` + `PrunePredicate` + the `RelayRun` mission machine + the `Brain` plan loop + the live why-view; one nullable column (`directives.sourceRelayCode`) is the whole schema change. Carries the verified eight-clause Robustness evidence map (with the rows whose evidence is NARROWER than the clause called out — clause 4's "no new poller" leg is structural not tested, and clause 6's "no budget burn" holds for the stall-retry path only, the transient-deferral path spending one `.high` read per tick forever). **The plan's prune predicate implemented literally would have reclaimed the ENTIRE mesh** (a deployed relay is a zero-cost SOURCE, never an interior node, and is filtered out of returned paths) — caught test-first, fixed by rooting the union at the anchor with relays as free interior nodes. Plan arithmetic corrected: the relay bill is **370 TOTAL across six types**, not 370 per type. Two capability facts to know before extending: ~~`returnToOrigin: false` strands the carrier~~ (CLOSED 2026-08-04 by [relay-return-and-restock](relay-return-and-restock.md) — the e2e no longer encodes the stranding), and the reclaim sequence is safe only because the carrier HOSTS a replicant (authority survives via physical presence, not the mesh, once the source relay is deactivated) — whose unrecorded mirror is that flying that carrier away must leave a STATIONARY replicant behind or rule-(2) authority dies for the whole mesh. **Ships INERT on today's fleet** (a stalled `salvageRun` holds the only hub carrier, and the brain may not touch a non-`relayRun` row), and **a second carrier is a GATE**: three separately-deferred items all arm in the same tick one arrives.
- [Stockpiles are brain value](brain-stockpile-value.md) — already-mined units awaiting a Haul Run now feed BOTH halves of `tendMesh`: prune pins the road to them (fifth `servedSystems` source) and grow ranks them at `ValueTier.stockpile`, between `moderateBelt` and `salvage`. Amends [brain-tendmesh-worthiness](brain-tendmesh-worthiness.md)'s five-tier order. Records why depletion and pile-creation are the same instant, and the deliberate no-minimum-pile-size call.
- [Relay return leg + restock](relay-return-and-restock.md) — SHIPPED 2026-08-04: the carrier flies itself home (to the hub LOCATION re-derived through `WorldView.hubLocation`, NEVER `originDesignation` — that is `SiteAssay.system(of:)`, and a bare system designation lands at an entry-point L4 away from the printer, which every e2e assertion but one still passes on), plus a persistent hub-owned `restockRun` printing against unmet demand at `idleCap = 10`. Restock BUYS its own census read (`.refreshFootprint`, `thenStall: nil`) because **nothing polls `LocationFootprint`** — `refreshFootprint()` has two callers, a mission's own action and the Locations screen — so the first cut, which waited for a fresh census, was inert outside the ≤60s window after some other mission refreshed. Safe to self-target only because the staleness gate is TABLE-WIDE (a successful refresh that still omits the hub is positive evidence and falls through to the fail-closed veto). **Also carries `idleCap = 10`'s arithmetic** (recovered from the source comment during the 2026-08-05 comment cleanup, previously recorded nowhere else): it is a CAPITAL ceiling — a relay is 370 units, so ten is 3,700 units parked — never a throughput throttle, because demand (`min(idleCap, targets.count)`) and then `aggregateSpendFloor` both bind first; re-derive from units-parked, not throughput.
- [Brain tunable calibrations](brain-tunable-calibrations.md) — the arithmetic behind the brain's two hand-tuned constants, recovered from source comments during the 2026-08-05 comment cleanup and previously recorded nowhere else. **`Brain.reclaimRangeLY` = 2 relay hops = 15 ly** — closes the "build-time distance cutoff" [brain-tendmesh-worthiness](brain-tendmesh-worthiness.md) deferred. Measured from the PLANT SITE, never the hub, because the triangle inequality bounds the reclaim detour by `2 · d(source,target)` wherever the hub stands (that rule stays in source); the NUMBER comes from the trade — a reclaim buys the fixed 370-unit relay bill and its ~800 s print, and costs at most `2 · d` of travel, so at ~30 s/ly pessimistic ([travel-is-cheap-vs-survey](travel-is-cheap-vs-survey.md): 1–3 min typical, 467 s worst observed) the `d = 15` bound is 30 ly ≈ 870 s, the same order as the print it replaces, with the resource side breaking the tie in reclaim's favour because `aggregateSpendFloor` sits at ~47% of live hub stock. Sanity: today's 15-relay mesh fits in a ~16 ly ball, so the cutoff bites only once the mesh outgrows that; re-measure travel before re-calibrating. Also records **`Brain.retryBudget` = 3**'s LOWER bound (attempt 1 = one-off, attempt 2 = slow-but-real recovery, and it is not 1 because escalation spends operator attention on transients the second attempt clears silently) — the upper bound's argument is still in source.
- [Brain `R` reserve-floor rail](brain-relay-reserve-floor.md) — automation-brain Task 20 (`BrainCeiling`), **updated post-review**: verified live 2026-08-03 — inventory lives at the LOCATION as `inventory: [{quantity, resource_type}]` (not a device field, not a dict); the real FTL-relay bill (`carbon 20, silicates 100, structural 80, rares 40, conductive 120, volatiles 10`) sums to **370 TOTAL across six types, never 370 per type**. `N` (idle-relay buffer cap) confirmed RETIRED in ticket 10 — no cap, no pool management built. `BrainCeiling`'s per-type floor is `K × relayBill(type)` (`K = 5`, marked `// CALIBRATE`, `resourceTypes` now DERIVED from `relayBill.keys` to close a fail-open hole an unbilled type would otherwise leave); `printPermitted(hubStock:)` — the real per-type `R` — fails **CLOSED** on a missing/empty stock reading. **Corrected the binding type: conductive (~107 relays' headroom) binds first, NOT volatiles (~407, actually last)** — absolute scarcity ≠ scarcity relative to consumption; the first note draft had this backwards. **Fixed a real gap**: `RelayRun` can't call the per-type check yet (no per-type stockpile record, ticket 06), so it arms a TOTAL-only proxy — the naive sum-of-floors (~1,850) first shipped undershot by ~18× (conductive actually exhausts at total stock ≈35,000), leaving `printPermitted` with zero production caller; replaced with `BrainCeiling.aggregateSpendFloor` (35,078, pinned in tests), derived as the total stock at which the reference mix's binding type would hit its own floor — fires no later than the true per-type rail. Renamed `totalReserveFloor`→`aggregateSpendFloor` since the old name overclaimed to be `R` itself. **Round 2**: the freshness gate `RelayRun.acquire` uses before trusting the census must be TABLE-WIDE (`world.footprints.values.map(\.fetchedAt).max()`, matching `HaulRun.survey` exactly), never per-location — a per-location gate self-loops unbounded (`.refreshFootprint(nextStep: .acquire)` targets its OWN step, `stepStartedAt` re-stamps on every re-entry with no same-step exception, so a persistently-missing row hammered the API every 5s tick forever, not "throttled to 60s" as first claimed). Fixed: gate on the whole table's freshest read, so "census refreshed and still doesn't list the hub" is POSITIVE EVIDENCE → falls through to `printStockIsShort` → veto → bounded-retry-then-escalate, proven terminating by a dedicated repeated-entry test. Also fixed a false arithmetic claim ("structural's floor is 15× the naive sum" — floor is 400, 0.22×; the true fact is structural's STOCK, 27,436, is ~15× the naive sum). Noted: `aggregateSpendFloor` (35,078) is ~47% of live hub total, so any non-print drawdown below that vetoes ALL printing regardless of per-type health — a real operational wall until the per-type record lands. **Round 3**: closed the whole-API-outage residual round 2 left out of scope — `MissionAction.refreshFootprint` gained `thenStall: DirectiveAttentionReason?`, resolved by a new `DirectiveEngineCore.resolveFootprintRefresh` that reuses the SAME `reAsk` collapse `.refreshDevices` uses (now generalised with an `orElse:` fallback so `HaulRun.survey`'s `nil`-thenStall can keep its own "always advance anyway" contract unchanged, while `RelayRun.acquire`'s real `.printStockShort` reason collapses to `.stall` — proven bounded end-to-end through the real engine, not a fixture, by a permanently-throwing-refresh test). `DirectiveExecutor.apply`'s `.refreshFootprint` case is now the same "engine already resolved this" bypass fallback the device-refresh cases share. Also closed a round-3-introduced minor: a hub row that stops refreshing while OTHER locations keep the table-wide gate looking fresh could have its stale `resources` reading trusted — fixed via a separate, read-time-only `hubFreshness`-bounded veto check inside `printStockIsShort` (does not reopen the refresh-trigger self-loop, since it never requests another refresh). **Round 4**: round 3's shared `reAsk` collapsed **any** of the four refresh kinds onto the reason of the refresh it had just paid for, so `RelayRun.acquire`'s ORDINARY path (stale hub DEVICE row → `.refreshDevices(.unreachableDevice)`; that read succeeds, re-ask then finds the census stale → `.refreshFootprint(.printStockShort)`) stalled `.unreachableDevice` on a reachable device and halted before the rail ever ran — a regression, since under round 2 it fell through to the executor's census refresh. Fixed: the collapse is now KIND-SCOPED via a `paid: Set<RefreshKind>` threaded through all four resolvers, a re-ask raising a kind not yet paid for CHAINS one hop into that kind's own resolver (passing it through unresolved is unsafe — `DirectiveExecutor`'s refresh case is a bypass that stalls instead of reading), and the stall reason/fallback is read off the RE-ASKED ACTION (`collapse(_:)`), never the caller's — which also retires round 3's `orElse:` parameter while preserving `HaulRun.survey`'s nil-fallback contract exactly. **Termination bound: at most one refresh round per KIND, ≤4 per evaluation** — `paid` grows strictly on every guarded hop over a closed four-case enum, so it depends on no read succeeding and no mission being well-behaved. The gap that let this through was structural: every `RelayRun` test was a pure-function table and NO test drove `RelayRun` through `DirectiveEngineCore` — now closed by `RelayRunEngineTests`. Minor also fixed: the veto log said `stock <n> below floor <f>` on all three `printStockIsShort` branches, false on two (an abundant-but-STALE 999,999 reading printed as though below 35,078); `printStockShortDiagnosis` now names the condition that fired. **2026-08-05**: records `K = reserveRelays = 5`'s own two-sided calibration (lower bound — 600 conductive units is ≈4.5% of measured stock, a real reserve; upper bound — every per-type floor sits far below holdings, so the rail is not a wall), recovered from the source comment during the comment cleanup.
- [Belt value vocabulary](belt-value-vocabulary.md) — automation-brain Task 9 (`BeltClass`), verified live 2026-08-03 via API probe + a 141-system/56-belt local aggregate: `Belt.density` has EXACTLY three live values (`sparse`/`moderate`/`dense`, no `rich`/`abundant`/`medium`/`thin`); `Belt.richness` (wire `resources`) qualifiers are `scarce`/`low`/`moderate`/`high`/`rich` (no `abundant`); real resource-type keys are `carbon`/`conductive`/`rares`/`silicates`/`structural`/`volatiles` (not `metal`/`silicon`). Locked mapping: `dense`→`.rich`, `moderate`→`.moderate`, `sparse`→`.sparse`; richness fallback folds `rich`/`high`→`.rich`, `moderate`→`.moderate`, `low`/`scarce`→`.sparse`; unresolved → `nil` (unknown stays unknown, never defaults to `.sparse`).
- [Brain resource-hub model](brain-resource-hub-model.md) — automation-brain ticket 06 (the last spine ticket): a hub is a DERIVED, RECOGNISED predicate (a commandable location with a print-capable device + adequate per-type stock), NOT placed/managed — single hub this effort (the one autofactory's location, meshed by anchor co-location); placement/multi-hub → growFleet-future. Print-in-situ REJECTED (salvage sites ≤3 types, a relay needs 6): printing is AT the hub with a HEAVEN Vessel co-located to auto-stow. Inventory: rank on per-type wherever known (SiteAssay + retained confirm-reads; totals fallback) via a small additive per-type stockpile record beyond totals-only LocationFootprint; the .high per-type /inventory read is the printStockShort rail veto and is retained. Supply = mine/salvage ARE the supply line feeding the hub via the haul engine: salvage = shipped Salvage+Haul Run (sink→hub), mine = a dedicated PERSISTENT Haul Run per site (never depletes; brain-derived statelessly; separate/decoupled; per-site device cost → growFleet trigger). The Relay Run = print+deliver, consuming hub stock, decoupled from resupply through the hub buffer (printStockShort→idle). D4 answered: shuttle collapses into a generalised Haul Run. Spend ceiling: R = per-type reserve floor rail, N = reclaim-fed idle-relay cap; literals deferred to build/runtime calibration. Off-mesh hub → escalate. Amends 05 + 04 + 03.
- [Salvage Run design](salvage-run-design.md) — BUILT 2026-07-30 (see the review-corrections section for the planner-hook seam, the idle-vs-complete decision, and the timeline-derived re-entry budget): Salvage Run + Haul Run as two uncoupled directive kinds (the split works only because planting a relay makes the freighter commandable without the miner parked alongside); the relay frontier bootstraps itself (9 relays → 10 of 13 systems, 15,650 of 20,471 units); plus the threads deliberately left unbuilt (engine-side printing/stowing/repair, tag-driven non-AMI automation, beacon emplacement for location events, waypoint relay errands). **Amended 2026-07-31**: vessel now tours the sites (new `positioning` step before `configuring`, keyed off `nextBody`) instead of ferrying drones from a parked vessel; `awaitCompletion` waits out mining state-driven (no blind 10-min backstop) so it no longer false-stalls `dronesNotRecovered`, with the fresh-evidence gate keyed off the drone rows via `min()`. **Also carries the run's tunable calibrations** (recovered from source comments during the 2026-08-05 comment cleanup, previously recorded nowhere else): `arrivalConfirmDeadline` = 5 min because that is roughly this account's device-row refresh period and the gate's own throttled read is the only thing that can advance `vessel.updatedAt`; `reconcileInterval` = 2 min as the two-sided trade between read cost over a multi-minute mine cycle and noticing a dropped `directive.completed` frame; `activationDeadline` (and `systemResolutionDeadline`) = 10 min, generous because the status flip depends on a poll-budget-subject confirm-read that a tighter deadline would false-stall ahead of. **Amended 2026-08-06**: the old "the blob's `depleted` momentarily lags, self-correcting via the stall" caveat was FALSE — `mergingScan` discarded the fresh salvage roster on any already-hydrated body, so `salvageBodyNotDepleted` was unclearable by Retry and only a hand tour of each site in the Locations UI could lower the flag; both roster paths now reconcile through `mergingSalvage(clearingDepleted:)` (a scan may clear, a roster only ever raises).
- [Directive log window + timeline collapsing](directive-log-window-and-timeline.md) — SHIPPED 2026-08-06, and it SUPERSEDES the per-directive-row-cap conclusion in [survey-fleet-repair-build](survey-fleet-repair-build.md): **bound the READ, never delete the rows and never stop writing them** (three loop bounds count exactly those `.stepStarted` rows). Measured live — one running `haulRun` held 9,071 entries growing ~680 in a few hours, 98.7% `.stepStarted`, ~132 bytes each, so disk was never the problem and re-reading them all every 5s tick was. `WorldSnapshot.log` is now the newest 500 ascending, with `auditLog` as an unwindowed but KIND-scoped escape hatch because the `.opCompleted` audit pass would otherwise silently never audit an op dispatched before the cutoff. Also records why the timeline collapses a repeating **cycle** and not a repeated step — the live Haul Run rotates `assigning → surveying → hauling` with ZERO adjacent duplicates, so the obvious consecutive-duplicate collapse was a measured no-op — and that the collapse prefers the SMALLEST period (a period-3 cycle also matches period 6) while comparing `kind` so a repoint breaks a cycle instead of vanishing into it.
- [Survey fleet repair build](survey-fleet-repair-build.md) — SHIPPED 2026-08-06: two service bots ride the survey vessel, deploy on arrival, hot-repair the drones during the survey, and are recalled before departure; repair is AUTONOMOUS (co-location is the whole trigger, the engine issues no `repair`), the gate exits on bot IDLENESS never a capacity threshold (`service` restores to an unquantified "functional level" a threshold could wait on forever), and `DirectiveLogRetention` lands beside `OperationRetention`. Carries the three traps that cost fix rounds — **`Device.location` is a SITE not a system** (`TAU-2` ≠ `TAU-9`, so an exact-match query loses a bot that cruised to do its job, releasing the gate mid-repair and abandoning the bots on departure), **`DirectiveExecutor.move` re-stamps `stepStartedAt` on every `.advanceStep`** so a dispatch/confirm PAIR that loops resets its own deadline and must be bounded off the directive's log instead, and **a shared test helper at internal scope captures other suites' call sites** (Swift prefers a zero-defaults candidate over a private one needing three, which silently rebound `HaulRunTests`/`RelayReturnAndRestockTests` and broke four tests). Also records why **the retention sweep does NOT bound `directiveLogEntries`** — it exempts open runs, and 8,394 of the 9,442 live rows are on one RUNNING `haulRun` — plus the ownership and `recall`-stows-on-nearest-craft preconditions that arm the moment a second bot-carrying fleet exists.
- [Haul Run design](haul-run-design.md) — SHIPPED 2026-07-31: the engine issues NO collect/deposit — the AMI transport controller hauls server-side via `ferry`, so the run only picks which pile each `auto:haul`-tagged controller drains and issues one `set_directive` per repoint (~1 command per pile vs ~28); supersedes §6 of the salvage design, needed no migration/client/table, and records the four live-fire defects review caught plus what deferred.
- [FTL authority rule](ftl-authority-rule.md) — command authority = a replicant PRESENT, or the target in a mesh subgraph containing one STATIONARY replicant; a relay must be IN a system to mesh it (7.5 ly is the relay-to-relay edge, not a coverage radius) and within a subgraph there are NO hops, so `ftlLinks` is a closure not a link list; the anchor replicant must never travel. **Rows now carry distanceLy/rangeA/rangeB (2026-08-01) — read the mesh through `DirectFTLLinks`, never off raw rows.**
- [Replication needs the `matrix` feature](replication-source-matrix-feature.md) — `replicate` is gated on the `matrix` device feature, and an `empty_replicant_matrix` LOSES that feature once replicated into, so a replicant born from replication can never itself replicate (only the account's original matrix can); undocumented, confirmed live 2026-07-31, and the reason the eligibility hint now names the real blocker.
- [Device tags + in_control_range](device-tags-and-control-range.md) — `GET devices/tags/{tag}` is fleet-wide so it SEES stowed and travelling devices that `?location=` structurally cannot (the root of both directive-engine incidents); `in_control_range` is already in openapi.json and the generated client but read by nothing.
- [Salvage resource amounts](salvage-resource-amounts.md) — SHIPPED 2026-07-25: SiteAssay store (original unit totals, merge-only-raises, keyed by site designation), payload-vs-envelope targeting rule for salvage events, "unknown never zero" optional-everywhere invariant, surfaced in the Locations catalog and the `gather_salvage` picker (ranked by units, Task 8).

- [Architecture review V3](architecture-review-v3.md) — 2026-07-20 five-axis review, **FULLY CLOSED 2026-07-28**: all four tranches (P0/P1/P2/P3) plus the last stragglers — V3.9 automation blockers 3–5 (answered by Directives; loop protection *obviated* by the clock-driven engine), S9 `new_device_code` CONFIRMED from the local ledger, T6 optionals done + Tagged/CommandParams decided against, LoggingMiddleware onto the house subsystem.
- [Event stream migration](event-stream-migration.md) — v2.3.0 native SSE stream replaced the custom relay; single-channel EventPipeline, dotted event taxonomy, EventRoute matchers, loud unhandled-event logging.
- [SSE typed event payloads](sse-typed-event-payloads.md) — design-confirmed 2026-08-03 (plan in docs/superpowers/plans/): migrate SSE payload extraction to one uniform standalone …EventPayload-per-event shape via a Utils reader; flagship = discovery-seeding (event.discovered carries the FULL quest shape incl. criteria — the LocationEventsIngestion comment is stale — so seed a complete row instead of reading accounts/events; progress is deferred to existing triggers, replay-safe).
- [Event Log feature](event-log-feature.md) — EventLogFeature: persisted SSE ledger window (Tools menu); write hook in EventRouter.dispatch; unhandled flag+filter; JSONTreeView now shared from UI. **The ledger query is capped at `displayLimit`** — an unbounded `@FetchAll` over the never-pruned table crashed the app with AttributeGraph "exhausted data space", because `SelectableList` (ScrollView+LazyVStack) measures every row.
- [Comment policy](comment-policy.md) — project rule (2026-08-05 DirectiveEngine comment cleanup, governs all modules going forward): a comment only explains code in situ, history goes to memory/git; `check-comments.sh` exit 0 is a floor, not proof — it's regexes over dates/device codes and can't see rationale. Only `DirectiveEngine` is swept so far.
- [Monospace system/location names](monospace-system-names.md) — project rule: any system/location name (designation code) always renders in a mono font token.
- [Lagrange points + entry_point](lagrange-points-and-entry-point.md) — the system-level `locations/{system}` endpoint returns NO per-planet Lagrange sites, so `Planet.lagrange` is empty for normally-fetched systems; every planet has 5 L-points by construction (`<planet>-L1..5`) and every system's `entry_point` is itself an L4 where a bare-designation travel lands the vessel. This silently made `SalvageRun.lagrangePoint` always nil → the run never planted a relay live (fixed 2026-07-31 to emplace at the entry point).

- [tendMesh relay pool + carrier tag](tendmesh-relay-pool-and-carrier-tag.md) — the 2026-08-04 live incident (3 runs, 3 untagged vessels, 2 stalled `noRelayCoLocated` beside 3 idle relays) and its fixes: idle hub relays are a POOL claimed by queue position (race-free because positions are disjoint; `.paused` holds no place in line), `auto:tendMesh` gates every carrier with NO fallback, and why a superseded print op used to strand a run forever.
- [Chrome min-height pins the window](chrome-min-height-window-pin.md) — a vertical `fixedSize` Text in NON-scrolling chrome (safeAreaInset, sidebar header) reports its zero-width wrap height as the view's minimum, which AppKit makes the WINDOW's minimum height: `BrainWhyView` pinned it at 4,014pt and killed vertical resizing. A List row never shows this (a scroll view clamps the min to nothing) and `navigationSplitViewColumnWidth(min:)` does NOT save you. Fix = keep `fixedSize`, add `lineLimit` (the `RCErrorBanner` idiom) — measured to change the render not at all. Includes the headless NSWindow-clamp measuring recipe, and two still-unfixed instances (`SidebarChrome.planRow`, `NoRelayBanner`).
- [List row preview crash](list-row-preview-crash.md) — Xcode 26 crashes List previews when the row struct is in the same file as #Preview; keep row structs in their own file.
- [Preview rewriter vs self.init](preview-rewriter-self-init.md) — a #Preview in the same file as a delegating convenience init fails the preview build; keep previews in their own file.
- [API module name](api-module-name.md) — package is "ReplicouldKit" but the importable module is "API".
- [OpenAPI operation-id format](openapi-operation-id-format.md) — runtime operationIDs are synthesized "method/path" strings (get/v1/stars), not Swift names; middleware must match Operations.X.id (the getV1Stars mismatch clamped the reads budget).
- [Ignore sandbox folder](ignore-sandbox-folder.md) — skip `Replicant/sandbox/` in all work unless explicitly asked.
- [pbxproj link is manual](pbxproj-link-is-manual.md) — linking a new SPM module product to the app target must be done by the user in Xcode; pbxproj edits are blocked.
- [New Star Map feature](new-star-map-feature.md) — NewStarMapFeature IS the Stars view; the SceneKit map and the "Stars (New)" split are gone. Structure, the three focus levels, the live-data path, and pointers to the module's README/HANDOFF (which hold the rendering invariants).
- [In-system viz plan](in-system-viz-plan.md) — COMPLETE (all 5 phases + deferred polish, 2026-07-14): devices at any location (incl. Lagrange), multi-leg travel, sites/inventory; keystone = the pure OrreryLayout resolver; execution record in the module's plan doc.
- [Star map live overlays](star-map-live-overlays.md) — current-location reticle, FTL mesh (real /network links), and ships are now sourced from Replicant/Device data (not faked); NewStarMapFeature is the only wired-in Stars view.
- [Orrery layout tuning](orrery-layout-tuning.md) — orrery sizing/orbit model: sun-relative sizes, non-overlapping spacedOrbits pass, fidelity over accuracy.
- [Orrery physical fidelity](orrery-physical-fidelity.md) — SHIPPED 2026-07-27: rings/axial tilt/rotation + the moon block (orbital_period_hours was undecoded, has_atmosphere is a bool not a string); retrograde falls out of pole geometry so `sign` must ignore obliquity; the body texturing frame MUST be right-handed (`cross(bx, pole)`) or every planet spins backwards; spin rate anchors on a GLOBAL 24h reference, never on neighbouring bodies; the `lon = atan2(...)` beach-ball trap that bit the molten style; and the body-level orbit freeze removed by tracking the drilled planet (orreryBuildCenter + fieldCenter make it free).
- [System→body drill rush](body-drill-rush.md) — SHIPPED 2026-07-28: the deeper drill is NOT a dive (camera ~17R out before, ~19R after — the zoom is faked by growing the central body), so fades/timing can never fix its feel; `OrreryPush` scales the SYSTEM layer out from the drilled planet (`bodyPush = 5`, Matt's number), folding into a layer's (centre, **reveal**) so no shader or buffer changes. **The k² trap:** OrreryLayout places at `sceneRadius · scale · reveal`, so pushing scale too squares the factor — it shipped that way, and the error term is a 30× radial-from-the-SUN expansion that detaches bodies from their rings and flings inner planets past the drilled one.
- [Flare playground](flare-playground.md) — animated solar flares at highest LOD; FlarePlayground tuning tool + FlareParams uniform kept in the module until user says remove.
- [Asteroid playground](asteroid-playground.md) — REMOVED 2026-07-28 once the impostor landed (restore from 54e4ff8); keeps the four things worth knowing before rebuilding one — drive the PRODUCTION body shaders not a copy, reproduce the two-pass HDR+tonemap, orreryAlpha must be 1, and camera distance ≥ ~5.1.
- [Nebula playground](nebula-playground.md) — NebulaField (3 styles + star diffusion) wired into the live map; standalone NebulaPlayground tool + NebulaUniforms struct removed 2026-07-20.
- [Planet texturing](planet-texturing.md) — orrery planets procedurally textured: PlanetType/PlanetMaterial → OrreryBodyUniform → orrery_body_fragment; style-index + tag-modifier sync points; atmosphere halo + animated clouds; feel constants now baked as kCloud*/kHalo* in Shaders.metal (OrreryTuning struct + sliders removed).
- [Metal HUD glass hitch](metal-hud-glass-hitch.md) — glass cards (ultraThinMaterial + big shadow) hitch the main-thread MTKView camera fly; drop the material/shadow, keep the animation.
- [Star map hydrate fly hitch](starmap-hydrate-fly-hitch.md) — drill-in fly hitched because a mid-fly hydrate DB write forced a StarSystem blob re-decode on the main thread; fix = defer the hydrate until the transition lands.
- [Star map main-thread load](starmap-main-thread-load.md) — the four costs that jammed other windows + lagged ship icons; round 1 lightening (frame pacing, label throttle, shared OrreryLayout, emission pixel-snapping, SystemDecodeCache) + round 2 (GPU-drawn ship icons replace the SwiftUI overlay; label selection off-main via nonisolated LabelSelection).
- [Metal SPM integration](metal-spm-integration.md) — how to wire a .metal shader + shared C struct header into an SPM library target (process resource, Bundle.module, C target, no defaultIsolation).
- [Civilisations feature](civilisations-feature.md) — Catalog › Civilisations = backend "species" + account reputation blended into one table (nullable totalReputation); cold-load vs reputation-only refresh split; star_regions spec patch.
- [Locations catalog feature](locations-catalog-feature.md) — Catalog › Locations disclosure tree; domain+persistence in UniverseModels, StarsClient/LocationsClient in GameServices (since 2026-07-21), UI in LocationsFeature; blob-per-system persistence, hydrate-on-select.
- [UniverseModels module](locations-catalog-feature.md) — pure models module (no clients since 2026-07-21): census Star/Position/Recon/LifeTier/InventoryItem + the locations domain; wire DTOs internal behind the public LocationDecoding facade.
- [Supervisor-adopts-row fails only whole-package](supervisor-adopts-row-whole-package-failure.md) — `theSupervisorAdoptsTheRowTheBrainLaunched` goes red only under `--build-system native` (all ~2,000 tests in one process), green per-product and in isolation; verified pre-existing at `a2f200a`, so don't attribute it to your change.
- [Running package tests](running-package-tests.md) — SPM module tests ARE in the Xcode test plan; schemes with test targets work as expected (`swift test` also works).
- [OpenAPI spec drift leniency](openapi-spec-drift-leniency.md) — POLICY: keep spec STRICT, do NOT relax additionalProperties; patch drift with targeted typed keys.
- [OpenAPI spec layout](openapi-spec-layout.md) — spec files in Modules/API/Sources/OpenAPI/ as pristine + -edits per version; openapi.json symlinks the active -edits.
- [Undocumented success response throws](undocumented-success-response-throws.md) — an operation declaring only `default` decodes its 200 against the STRICT error schema, so every success throws (quest completion, 2026-07-30).
- [Device command shapes](device-command-shapes.md) — per-command POST response classes (travel=deadline, mine=continuous, scan/census=synchronous, print=enqueued, lifecycle=immediate) + mining lifecycle.
- [Device command taxonomy](device-command-taxonomy.md) — full command inventory + feature gates, live-roster coverage (2026-07-22), unsurfaced commands to fold in (configure/message/replicate/repair; drop detonate), and the approved 9-group grouping + sheet/inline/confirm presentation rule for the DeviceDetailView revamp.
- [controlled_devices is detail-only](controlled-devices-detail-only.md) — `controlled_devices` ships only in GET devices/{code}; the fleet-wide list omits it AND a list sync erases it from `detail`, so read adoption from the drone's `controller_device_code` column (this hid every staged vessel from the Survey Run launcher).
- [Location scope cannot see stowed](location-scope-cannot-see-stowed.md) — `GET devices?location=X` answers PRESENCE and stowing CLEARS location, so a stowed device is out of scope, not stale (in-transit ones ARE returned, which is what misleads); this froze a Survey Run in `recovering` for 20min at ESELLUSAU with every drone already aboard, re-probing every tick because the throttle keyed on rows the probe never wrote — containment questions must name the device.
- [Staging freshness vs read budget](staging-freshness-vs-read-budget.md) — a mission's WorldSnapshot is local SQLite only, so a `.low` confirm-read deferred by the budget floor made Survey Run preflight read stale silence as "not staged" (and made Retry a structural no-op); fixed by folding stow state out of `device.stowed`/`device.deployed` and by `MissionAction.refreshDevices`.
- [Paged endpoint maxima](paged-endpoint-maxima.md) — always send the max `limit`: 100 everywhere, 50 for `GET devices`; over-asking is silently clamped (not rejected), the spec documents no maxima, and the bare default is 20 — which truncates probes.
- [Location sites endpoint](location-sites-endpoint.md) — GET locations/{designation} polymorphic per location_type; mining sites vs salvage sites, belt inventory, explored-gating; root of the Locations catalog.
- [Feature module import GameSession](feature-module-import-dependencyclients.md) — a new module's domain client needs `import GameSession` (gameClient's home since the 2026-07-21 M2 split; before that GameServices/DependencyClients) for `@Dependency(\.gameClient)`; missing it shows as a misleading key-path inference error.
- [Travel is cheap vs survey](travel-is-cheap-vs-survey.md) — measured: travel ETAs are 1–3 min (max 467s observed) against survey cycles of tens of minutes, so survey automation should optimise COVERAGE, not route length; read durations off `completesAt`, never `lastConfirmedAt`.
- [Operations table retention](operations-table-retention.md) — nothing pruned `operations` until 2026-07-27 and every UI surface fetches it unfiltered; `OperationRetention` now drops terminal ops older than 7d hourly off `DeadlineScheduler.run()` (open ops never pruned, aged on `startedAt` not `lastConfirmedAt`).
- [AMI drones are event-silent](ami-drones-are-event-silent.md) — an adopted drone emits ZERO per-device events (all rolled into the controller's `ami.*.digest`), so it is never marked stale and its stow/location columns only move when something explicitly reads it; never wait on one to change by itself, never trust an unread positive containment claim, and use `ami.launched`'s `devices_deployed` as the one free launch signal.
- [Survey digest scans](survey-digest-scans.md) — SHIPPED 2026-07-27: `ami.survey.digest`'s v2.3.3 `report.scans[]` is the ONLY channel carrying a Survey Run's per-body scan intel (adopted drones are event-silent); the `moons` roster is a SIBLING of `planet` (so `RawScanEventResult` can't be reused), a scan is a PARTIAL observation so it needs `BodyObservation`/`observing(_:)` rather than `BodyDetail`/`applying` (which would erase sites/devices/inventory), salvage keeps existing `remainingPct` but takes fresh `depleted`; added `BodyPhysical.speciesName` + `Moon.lifeStage` (the latter was dropped on every path, incl. the orrery's hardcoded nil).
- [Survey search op shapes](survey-search-op-shapes.md) — survey-drone `search`: `scan` block (eta_seconds, no completes_at) + `scan_complete` relay event; wired as `OperationKind.search`.
- [Travel block leg vs route](travel-block-leg-vs-route.md) — travel block has per-leg (arrives_at/eta_seconds) vs whole-route (final_arrives_at/route_eta_seconds) fields; `device_travel_arrived` completes the trip (per-leg cruise/surge events don't).
- [Travel system-proxy codes](travel-system-proxy-codes.md) — a bare system designation in a travel payload is a PROXY for that system's entry point (no hyphen = proxy); OrreryLayout resolves it to the sun, which is why the map's transit riser disagreed with the sidebar. `TravelSnapshot.itinerary(stored:live:)` + `.resolvingSystemProxies` is now the one rule all three travel surfaces read; the orrery callout also counts down to this view's boundary (enters in / leaves in) rather than final arrival.
- [SwiftUI View statics trap in tests](swiftui-view-statics-trap-in-tests.md) — pure logic as a static/nested type on a SwiftUI View traps (signal 5) under `swift test`; extract to a plain SwiftUI-free namespace.
- [Device inspector refresh loop](device-inspector-refresh-loop.md) — while-viewing in-place refresh for mining/diverting devices; cadence in `DevicesFeature.refreshDelay`; mining-vs-seeking = pending_* > 0.
- [Replicants feature](replicants-feature.md) — ReplicantsFeature (galaxy directory) + KnownReplicant table (separate from Replicant roster); last-known-location from system_scan replicants block, hooked in CommandClient.
- [Bobnet feature](bobnet-feature.md) — BobnetFeature 3-panel channels/messages; BobnetChannel markers; relay catch-up (forward cursor); send via active replicant; 3s linger read rule. **`isAtLatest` must never be sourced from scroll geometry alone** (2026-07-31): `onScrollGeometryChange` fires only on *change*, so the opening at-the-bottom state is never announced and every switched-to channel's unread count froze; `.id(channel)` does NOT re-fire it (the modifier sits above the `.id`, keeping its stored value). Establish the flag on selection/appearance, let geometry only maintain it.
- [List query in state](list-feature-query-in-state.md) — standard: SQLiteData @FetchAll lives in TCA @ObservableState (not the view), view is a pure renderer; kills empty-state flashes.
- [A @Shared value is not a binding](shared-value-is-not-a-binding.md) — driving a `@Shared` property through `BindingReducer()` compiles and passes but hits Sharing's DEPRECATED setter (warnings don't fail the build, so nothing catches it); write it in a real action via `state.$x.withLock`, hand the view a `Binding` that sends that action, and keep `@ObservationStateIgnored` on the property. Also: `@Shared` tests are auto-insulated per test, so no hand-rolled `UserDefaults(suiteName:)` — which is what makes a persistence round-trip assertable inside one test.
- [Sidebar feature](sidebar-feature.md) — SidebarFeature owns the left column (header+category list+footer+category selection); MainFeature is now a thin container; progress driven by the Operation table.
- [Account feature](account-feature.md) — AccountFeature module: Profile/Settings/Achievements sheet; consumes achievements + PATCH accounts/me; presented from MainFeature; notifications matrix + email editing deferred.
- [Device refresher dependency](device-refresher-dependency.md) — all device confirm-reads funnel through @Dependency(\.deviceRefresher) over one shared PollCoordinator (in GameServices).
- [Location endpoint presence gate](location-endpoint-presence-gate.md) — CORRECTED 2026-07-27: GET locations/{designation} is gated on EXPLORATION, not presence; the 403 "No replicant in system" message lies, and any explored system rehydrates from anywhere.
- [Census explored is not distance-sorted](census-explored-is-not-distance-sorted.md) — the census sorts by CURRENT distance while `explored` is HISTORY, so explored systems scatter across all 141 pages (SOL was on p13); never stop the overlay walk early, and GET /v1/locations is the cheap partial repair.
- [Location Events feature](location-events-feature.md) — Missions/quest-log; accounts/events is the account-wide list w/ full detail; LocationEvent table in GameModels; Event Log renamed Operations Log.
- [Experience + completion events](experience-and-completion-events.md) — SHIPPED 2026-07-30: a quest's `rewards.xp` is DOUBLE-delivered (also as `experience.gained` with `source: "location_event"`), so `event.completed` is display-only; XP credits on `.stream` provenance ONLY (catch-up is already in the launch accounts/me read) then reconciles via a droppable `.account` domain gated on the reads budget; and `accounts/events` NEVER returns `consumed`, so `merging` must carry a captured manifest across or the Consumed card vanishes on the next refresh.
- [GameDatabase module](gamedatabase-module.md) — single schema-composition point; previews/tests call `GameDatabase.bootstrap()` instead of hand-rolling migrators.
- [Erase-on-schema-change](erase-on-schema-change.md) — why adding a table used to wipe the DB (GRDB compares against the last APPLIED identifier, so mid-list migrations erased); replaced 2026-07-26 by the append-only `GameDatabase.manifest` + `DatabaseReset`.
- [Locations list flatten perf](locations-list-flatten-perf.md) — big macOS hierarchical list: flatten to visible rows + flat List beats children:/DisclosureGroup (generic-metadata/ARC storm at 5,770 nodes).
- [SQLite DB location](sqlite-db-location.md) — path to the running app DB for direct sqlite3 inspection; census `stars` lags `systemDetails`
- [Location response schema drift](location-response-schema-drift.md) — openapi.json patch so GET locations/{designation} decodes (asteroid_belt); re-apply after re-fetch
- [Decode diagnostics decorator](decode-diagnostics-decorator.md) — OpenAPI decode errors logged centrally via DiagnosticAPIClient (APIProtocol decorator); regen with scripts/gen-diagnostic-client.py; call sites stay clean
- [SPM stale layout crash](spm-stale-layout-crash.md) — RETESTED 2026-07-25: does NOT reproduce under the default swiftbuild engine; the rm -rf Modules/.build ritual is obsolete for swift test (was a deprecated-llbuild artifact). Xcode app-target half still stands.
- [API drift backlog](api-drift-backlog.md) — no known open drifts as of 2026-07-20; swift test fully green (records the resolved new_resource/terminating/adopt-read fixes).
- [Preview sheets need .sheet(item:)](preview-sheet-item-not-ispresented.md) — travel/print preview sheets must use .sheet(item:) not isPresented, else rapid back-to-back presentations drop the dialog; fixed in 3 travel entry points + effect cancellation.
- [SourceKit-LSP index reality](sourcekit-lsp-warmup.md) — FIXED 2026-07-25: .sourcekit-lsp/config.json points LSP at SwiftPM's swiftbuild BSP server so it reads the index your own builds write (0 → 45 refs on same-day code); needs scripts/link-index-store.sh once per checkout because upstream advertises .build/index-store while builds write .build/out. Note the index is now only as fresh as your last build.
