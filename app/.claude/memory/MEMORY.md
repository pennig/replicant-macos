# Memory Index

- [Active replicant, not first](active-replicant-not-first.md) — multi-replicant invariant: never `roster.first` as a proxy for the active replicant; resolve via `activeReplicantCode`, and fan event ingestion out over the whole roster.

- [Same-step dispatch needs a tracked op](same-step-dispatch-needs-tracked-op.md) — a step dispatching at its OWN step is safe only for tracked op kinds; a `.simple` verb creates no Operation, so the guard is structurally nil and `stepStartedAt` re-stamps forever. Split dispatch from polling.
- [Confirm steps need fresh evidence](confirm-steps-need-fresh-evidence.md) — a polling step judging a local device row must prove `updatedAt >= stepStartedAt`, and the staleness guard must sit AFTER the deadline check. Half three: a DISPATCH step needs the same proof, keyed off the ARRIVAL.
- [Arrival is one transaction](arrival-single-transaction.md) — `Reconciler.applyDeviceEvent` closes the op and patches `device.location` in ONE write, closing the two-transaction race. Op-closing patches unconditionally; the non-closing path keeps a 1s-tolerance guard.
- [Launcher tag-resolution error narrowing](launcher-tag-resolution-error-narrowing.md) — the three operator launchers' `try?` collapsed a real DB error and the ordinary no-theatre case into one silent fallback; now logged distinctly. Diagnostics only, stamped tag unchanged.
- [Directives feature](directives-feature.md) — automations v2: the shipped history of Stages 1–5, stall resolution, the step timeline and the continuous survey roam. Survey Run NEVER stows or adopts. Carries the engine's standing invariants — don't undo them.
- [SystemDetail.persist choke point](system-detail-persist-choke-point.md) — never `SystemDetail.upsert` in production; go through `persist`, which also stamps the write-once `stars.fullyScannedAt`. `StarSystem.isFullyScanned` is the one completeness definition.
- [Brain ↔ executor seam](brain-executor-seam.md) — ticket 04: the brain's whole vocabulary against the engine is create / cancel / drive the sanctioned retry-cancel verbs. Running directive rows ARE the lease ledger; stall response is classified by `brainDisposition`.
- [Brain goal & decision policy](brain-goal-decision-policy.md) — ticket 03: the brain is a pure selector deriving goals per tick and allocating in one greedy lexicographic pass. Five goal kinds; print/deliver/shuttle/repair are engines, not goals.
- [Brain robustness bar](brain-robustness-bar.md) — the eight-clause definition of done every automation-brain capability must answer with evidence in review. Spine: the brain is a pure selector over the existing safe rails, stateless between ticks.
- [Brain primitive contracts](brain-primitive-contracts.md) — ticket 05: print/deliver/shuttle are ENGINES, not goals — step-libraries composing into one carrier-owned Relay Run. They FIT the existing engine; the carrier is the only lease and the printer is never leased.
- [Brain tendMesh worthiness](brain-tendmesh-worthiness.md) — ticket 10, the last spine decision: `tendMesh` is the SOLE mesh authority. Grow is pathfinding toward known value, prune the same read inversely, so thrash is structurally impossible. Reclaim is lazy.
- [tendMesh build record](brain-tendmesh-build.md) — SHIPPED 2026-08-04, the brain's first ACTING capability. The plan's prune predicate implemented literally would have reclaimed the ENTIRE mesh. Carries the eight-clause evidence map and why a SECOND CARRIER is a gate, not a convenience.
- [Stockpiles are brain value](brain-stockpile-value.md) — already-mined units awaiting a Haul Run feed both halves of `tendMesh`; depletion and pile-creation are the same instant, which is what used to strand the units. No minimum pile size, deliberately.
- [Relay return leg + restock](relay-return-and-restock.md) — SHIPPED 2026-08-04: the carrier flies home to the hub LOCATION re-derived every time, never `originDesignation`. Restock BUYS its own census read because nothing polls `LocationFootprint`. Carries `idleCap = 10`'s arithmetic.
- [Brain tunable calibrations](brain-tunable-calibrations.md) — the arithmetic behind `Brain.reclaimRangeLY` (2 relay hops = 15 ly, measured from the plant site) and `Brain.retryBudget` (3, a ~30-minute fuse, not ~45). Where a re-calibration starts.
- [Brain `R` reserve-floor rail](brain-relay-reserve-floor.md) — `BrainCeiling`'s print veto: the relay bill is 370 TOTAL across six types (never per type), conductive binds first, and the print rail arms the coarse `aggregateSpendFloor` (35,078, ~47% of hub stock). Four review rounds on the freshness gate and the kind-scoped refresh chain.
- [Belt value vocabulary](belt-value-vocabulary.md) — probed live: `Belt.density` has exactly three values, `richness` five qualifiers (no `abundant`), and six real resource-type keys. Carries the locked `BeltClass` mapping; unresolved stays nil, never `.sparse`.
- [Brain resource-hub model](brain-resource-hub-model.md) — ticket 06: a hub is a DERIVED predicate, not a placed object; print-in-situ rejected; mine/salvage ARE the supply line. SUPERSEDED on hub-count by [theatre-recognition-model](theatre-recognition-model.md).
- [Theatre recognition model](theatre-recognition-model.md) — theatres are RECOGNISED every tick, never placed: pinned → systemHub → derived, first match wins, identity is the depot location and is now PERSISTED. A dry persisted depot keeps its identity and the run goes silent — parked for Matt.
- [Theatre component vs distance](theatre-component-vs-distance.md) — inward (which theatre owns this system) filters by mesh COMPONENT before ranking by distance; outward (where a new theatre should go) ranks by raw distance with no filter. The live mesh is one component.
- [Theatre-aware readiness build](theatre-aware-readiness-build.md) — the build making all four readiness functions per-theatre. Readiness scoping ALONE can never let a second theatre launch, and a per-theatre tag must NEVER reach the server. Operator migration still not done.
- [Haul round-trip ranking](haul-round-trip-ranking.md) — piles rank by units per round-trip second, same-system `.infinity`. `secondsPerLy = 30` is UNCALIBRATED; raw units survive as a per-candidate fallback for a missing position, not a global sort-mode switch.
- [Salvage Run design](salvage-run-design.md) — BUILT 2026-07-30, then amended four times as the same depletion stall fired three more ways (a merge discard, SSE propagation lag, and the server DELISTING a drained site). Carries the run's tunable calibrations and what was deliberately left unbuilt.
- [Brain survey goal build](brain-survey-goal-build.md) — SHIPPED 2026-08-06: the brain keeps exactly ONE roaming Survey Run alive rather than scheduling surveys. `surveyReadiness` has no stall case, so it cannot escalate by construction. Started the clock on the `systemJSON` decode hatch.
- [Brain salvage + haul build](brain-salvage-build.md) — SHIPPED 2026-08-07, the brain's third and fourth capabilities. The plan was wrong twice, and review caught a live double-commit in `ensureOne`. Records the retry-amplification residual and what the ticket-10 surgery cost the fleet.
- [Brain mine goal build](brain-mine-build.md) — SHIPPED 2026-08-09, a permanent mine via `mineFleetPrint` + `mineRun`. The plan's `confirmingAttach` ladder was unimplementable. Four live defects, all fixed — the over-print one's cause was rewritten by DB verification.
- [Brain event-fulfilment build](brain-event-fulfilment-build.md) — the brain's sixth capability, run live 2026-08-15/16. Two self-locking gates, and the Critical FIFTEEN task reviews missed because every fixture held one `matrix_container` named COURIER — the convoy would have flown the account's own replicant host to an event.
- [Printer selection ignored status](printer-selection-ignores-status.md) — a compacted, in-flight hub still advertises `enqueue_print`, so capability-only selection pinned it and Retry/Skip could not clear it. Amended: the substitute must also be FREE, and a deadline gated on `openOperation` cannot fire on a shared bench.
- [Demand-derived mine ranking](demand-derived-mine-ranking.md) — mine siting now weights belts by stock ÷ demand (open-event criteria priced through blueprint bills + the print reserve), not fixed rares/conductive constants; records why "volatiles is last in collections" was the WRONG signal (~11× covered) and silicates/conductive actually bind.
- [One op, many print jobs](one-op-many-print-jobs.md) — an `Operation` records one dispatched COMMAND and `enqueue_print` carries `quantity: N`, so jobs 2…N are adopted from the snapshot, not promoted; adoption dropped `directiveID` until `Reconciler.batchOwner`. Why the adopted row must stay untyped, and why `PrintQueueOwners` still answers by bench rather than by job.
- [The `printing` block carries tags](printing-block-carries-tags.md) — the ACTIVE print's tags are documented in openapi (back to 2.1.1) and populated live; `PrintingSnapshot` simply never parsed them. The queued tags the UI always showed are the UNdocumented ones.
- [Blueprint components build](blueprint-components.md) — SHIPPED 2026-08-15: blueprints that consume other printed devices. Ten defects, including a `waiting_for` nesting bug that drew a green tick on a blocked print. The fixture that hid three Criticals had one printer against a four-autofactory depot.
- [Directive log window + timeline collapsing](directive-log-window-and-timeline.md) — bound the READ, never delete the rows and never stop writing them: three loop bounds count exactly those `.stepStarted` entries. The timeline collapses a repeating CYCLE, not a repeated step.
- [Directives list grouping](directives-list-grouping.md) — the list groups by the `auto:<name>[:<site>]` fleet tag. Grouping must take the MOST SPECIFIC tag, and a mission-driven built-in row joins its MISSION. The fixtures could not find the first trap; replaying over the live SQLite rows did.
- [Evaluation writes only its own columns](directive-commit-column-ownership.md) — the executor's value is read at TICK time and written back outside any re-reading transaction, so a whole-row upsert DESTROYED an operator's pause; carries the three disjoint column sets, why a progress write must land on a paused row anyway, and why a refused lifecycle write drops its log entries.
- [Directive soft delete + retention](directive-soft-delete-retention.md) — Clear MARKS; a one-month purge on two triggers deletes. The terminal set is not a parameter — a safety property, since open rows own physical devices. Marking must never re-stamp `updatedAt`.
- [Survey fleet repair build](survey-fleet-repair-build.md) — SHIPPED 2026-08-06: service bots ride the survey vessel and hot-repair the drones. Three traps cost fix rounds, `Device.location` being a SITE not a system the worst. Deploying a bot is not enough — it lands paused.
- [Salvage fleet repair build](salvage-fleet-repair-build.md) — bots tour with the Salvage Run, deployed ONCE PER SYSTEM. No exit from a target system may reach `.advanceTarget` directly. Closes the cross-fleet `recall` residual via the `auto:`-tag ownership predicate.
- [Survey repair fleet tag](survey-repair-fleet-tag.md) — the 2026-08-09 fix for two days of silent no-repair after the survey bots were tagged. The diagnostic tell: `stepStarted` keeps logging while the confirm pairs' `commandDispatched` freezes. Any bot-carrying mission must pass an owner from day one.
- [A recall clears the bot's location](recall-clears-the-bots-location.md) — a recalled bot carries `location: nil` for its whole cruise, so the query deciding whether anyone is still out drops the device the question is about, and the guard written for it could never fire. A departure decision must select by stow, never location.
- [Survey Run owns the system scan](survey-run-owns-the-system-scan.md) — scan counts reach `locations/{star}` only after `POST replicants/{code}/scan`, which nothing in `SurveyRun` ever called, making Retry a structural no-op. Also: `Path:` in a swift-openapi decode error names the REJECTED KEY.
- [Survey completion authority](survey-completion-authority.md) — the counts can read complete while the AMI controller still has hours to run; the controller's own re-stowed row is the completion authority and counts only cross-check. Two accepted conservative halts.
- [Salvage controller recall race](salvage-controller-recall-race.md) — `directive.completed` tracks the DRONES, so the controller's own recall leg is still airborne when `verify` releases the vessel, and the stow ending the chase PAUSES the directive. A directive NAME is not a directive that is running.
- [Carrier hull capability gate](carrier-hull-capability-gate.md) — a `heaven_vessel` TYPE gate hid a tagged, fully-capable racing vessel and the blocker blamed tagging. Carrier selection now keys on `Device.isCarrierHull` (cradle+surge), and blockers name mistagged devices fleet-wide.
- [Haul Run design](haul-run-design.md) — SHIPPED 2026-07-31: the engine issues NO collect/deposit — the AMI controller hauls server-side via `ferry`, so the run only picks which pile each controller drains. Four live-fire defects, plus two vacuous regression tests that shipped.
- [Mine run per-theatre tag](mine-run-per-theatre-tag.md) — `ensureMine` stamped the bare `auto:mine` tag, so `reservedDevices` swept every theatre's mine devices while any install ran. Safe to fix because `MineRun`'s own fleet queries never read `directive.fleetTag`.
- [Haul depots are sinks](haul-depots-are-sinks.md) — one theatre's haul run drained the operator's stock out of another's freshly-pinned depot; every recognised theatre's depot is now excluded. The ENGINE guard is the one that counts, and its first cut passed against the broken code.
- [Re-tagging is the haul handover](haul-retag-is-the-handover.md) — re-tagging a controller did nothing, defeated three times over: the bare-tag fallback, a theatre filter judging by LOCATION, and both device columns reserving it account-wide. An explicit per-theatre tag is now authoritative at all three.
- [Haul Run theatre-scoped controllers](haul-run-theatre-scoped-controllers.md) — with two bare-tagged fleets each theatre's query matched BOTH controllers and each run repointed the other's cargo every tick, unbounded. Fixed by a `theatreDepot:` filter; amended so location decides bare tags only.
- [Logistics haul yields](logistics-haul-yields.md) — yields are RECONSTRUCTED from digest deltas, never reported. Carries the categorical palette's whole history, including the pair that measures weak and is DELIBERATELY KEPT because Matt's eyes outrank the model.
- [Logistics window aggregation](logistics-window-aggregation.md) — the charts fold in SQLite via `HaulYieldDigest`, never from a bounded row array: a 1,000-row `@FetchAll` was under two days of trips, so every range charted the same truncated slice. A table bound is NOT a chart bound; `State.init` now needs `$0.date`.
- [FTL mesh incremental fold](ftl-mesh-incremental-fold.md) — a `relay.*` event folds in ONE relay's view instead of sweeping every relay, and the roster trigger folds too, retiring this note's own claim that it couldn't. The split check MUST use the raw `isDirect` filter — the parity repair answers it by construction.
- [FTL authority rule](ftl-authority-rule.md) — authority = a replicant PRESENT, or the target in a mesh subgraph holding one STATIONARY replicant. A relay must be IN a system to mesh it; 7.5 ly is the edge, not a radius. Read the mesh through `DirectFTLLinks`, never raw rows.
- [Replication needs the `matrix` feature](replication-source-matrix-feature.md) — an `empty_replicant_matrix` LOSES the feature once replicated into, so a replicant born from replication can never itself replicate. Undocumented; re-probe before assuming exponential growth.
- [Adopt is feature-gated](adopt-is-feature-gated.md) — a controller adopts any device carrying its feature, never one named worker type, and there is no `transport_drone` — which hid Adopt permanently on every transport controller. The command grid is not `available_commands` alone.
- [Device tags + in_control_range](device-tags-and-control-range.md) — `GET devices/tags/{tag}` is fleet-wide so it SEES stowed and travelling devices that `?location=` structurally cannot (the root of both directive-engine incidents); `in_control_range` is already in openapi.json and the generated client but read by nothing.
- [Salvage resource amounts](salvage-resource-amounts.md) — SHIPPED 2026-07-25: SiteAssay store (original unit totals, merge-only-raises, keyed by site designation), payload-vs-envelope targeting rule for salvage events, "unknown never zero" optional-everywhere invariant, surfaced in the Locations catalog and the `gather_salvage` picker (ranked by units, Task 8).

- [Architecture review V3](architecture-review-v3.md) — the 2026-07-20 five-axis review, FULLY CLOSED 2026-07-28. Nothing from V3 is open; the note records what each tranche established as standing invariants.
- [Event stream migration](event-stream-migration.md) — v2.3.0 native SSE stream replaced the custom relay; single-channel EventPipeline, dotted event taxonomy, EventRoute matchers, loud unhandled-event logging.
- [SSE typed event payloads](sse-typed-event-payloads.md) — design-confirmed, not yet built: one standalone `…EventPayload` per extracting event via a shared Utils reader. Flagship is discovery-seeding — `event.discovered` carries the full quest shape, so the comment saying otherwise is stale.
- [Event Log feature](event-log-feature.md) — the persisted SSE ledger window, and the one window that has produced the AttributeGraph SIGABRT TWICE in two different views, plus a third non-crashing pathology. Carries the field diagnostic and why `heap`/`leaks` cannot census this process.
- [@FetchAll fetches on the writer](fetchall-fetches-on-the-writer.md) — every observed query re-runs INSIDE the writer queue, so an expensive `@FetchAll` charges its cost to every write to the tables it reads. Carries the attribution recipe and a corrected Locations attribution that reversed the first draft.
- [DirectivesFeature.body type-check budget](directives-body-type-check-budget.md) — `body` sat one declaration away from the compiler's time budget, so adding a func to `GameModels` failed the build in a file that change never touched. Split the expression, don't revert the innocent change.
- [Comment policy](comment-policy.md) — project rule (2026-08-05 DirectiveEngine comment cleanup, governs all modules going forward): a comment only explains code in situ, history goes to memory/git; `check-comments.sh` exit 0 is a floor, not proof — it's regexes over dates/device codes and can't see rationale. Only `DirectiveEngine` is swept so far.
- [`prunableWorld` fixture contract](prunable-world-fixture-contract.md) — `meshSystems` is DERIVED from `relays` via the real predicate, never hand-set, or a fixture fakes mesh membership; `replicants` defaults to EMPTY because multi-theatre broke hub/replicant co-location.
- [Monospace system/location names](monospace-system-names.md) — project rule: any system/location name (designation code) always renders in a mono font token.
- [Lagrange points + entry_point](lagrange-points-and-entry-point.md) — the system-level endpoint returns no per-planet Lagrange sites, so `Planet.lagrange` is always empty; every planet has 5 by construction and every `entry_point` is itself an L4. This silently meant the Salvage Run never planted a relay.

- [tendMesh relay pool + carrier tag](tendmesh-relay-pool-and-carrier-tag.md) — the 2026-08-04 incident: two runs stalled beside three idle relays. Idle relays are a POOL claimed by queue position, and the server LOWERCASES every tag. Amended — the claim was aspirational until it became a real column.
- [`WorldSnapshot.peers` and the positional relay claim](world-snapshot-peers-fifo.md) — `peers` answers "who else is competing for it?", not "what is the world like?"; `RelayRun.queuePosition` + `claimableRelay` claim POSITIONALLY (concurrent runs take DISJOINT relays by index into the pool, never a yes/no turn), and one `WorldTick` read now feeds every executor so every run computes that position from identical inputs. There is no `isNextInLine`.
- [Chrome min-height pins the window](chrome-min-height-window-pin.md) — a vertical `fixedSize` Text in NON-scrolling chrome reports its zero-width wrap height as the window's minimum: 4,014pt and no vertical resizing. Fix is `lineLimit`, measured to change the render not at all. Two instances still unfixed.
- [Scroll anchor forces a full ForEach walk](scroll-anchor-forces-foreach-walk.md) — `LazyVStack` is lazy about row bodies but `.defaultScrollAnchor(.bottom)` walks every element, so anything O(n) in the closure is O(n²). An isolated harness measures the algorithm, never the store — it understated this ~30× and nearly buried it.
- [Headless SwiftUI render probe](headless-swiftui-render-probe.md) — render any view (Charts included) to PNG from a temporary test file via NSHostingView + cacheDisplay, so a chart change can be LOOKED AT from a background job; needs a runloop turn and an explicit surface background or dark mode renders white-on-white and reads as missing text.
- [List row preview crash](list-row-preview-crash.md) — Xcode 26 crashes List previews when the row struct is in the same file as #Preview; keep row structs in their own file.
- [Preview rewriter vs self.init](preview-rewriter-self-init.md) — a #Preview in the same file as a delegating convenience init fails the preview build; keep previews in their own file.
- [API module name](api-module-name.md) — package is "ReplicouldKit" but the importable module is "API".
- [Rate-limit bucket hygiene](rate-limit-bucket-hygiene.md) — two governor defects: a foreign limiter's headers overwriting a shared bucket's pinned limit, and `min()` being blind to the server's window refill. Also why the brain must report the READS budget beside commands.
- [OpenAPI operation-id format](openapi-operation-id-format.md) — runtime operationIDs are synthesized "method/path" strings (get/v1/stars), not Swift names; middleware must match Operations.X.id (the getV1Stars mismatch clamped the reads budget).
- [Ignore sandbox folder](ignore-sandbox-folder.md) — skip `Replicant/sandbox/` in all work unless explicitly asked.
- [pbxproj link is manual](pbxproj-link-is-manual.md) — linking a new SPM module product to the app target must be done by the user in Xcode; pbxproj edits are blocked.
- [New Star Map feature](new-star-map-feature.md) — NewStarMapFeature IS the Stars view; the SceneKit map and the "Stars (New)" split are gone. Structure, the three focus levels, the live-data path, and pointers to the module's README/HANDOFF (which hold the rendering invariants).
- [In-system viz plan](in-system-viz-plan.md) — COMPLETE (all 5 phases + deferred polish, 2026-07-14): devices at any location (incl. Lagrange), multi-leg travel, sites/inventory; keystone = the pure OrreryLayout resolver; execution record in the module's plan doc.
- [Star map live overlays](star-map-live-overlays.md) — current-location reticle, FTL mesh (real /network links), and ships are now sourced from Replicant/Device data (not faked); NewStarMapFeature is the only wired-in Stars view.
- [Orrery layout tuning](orrery-layout-tuning.md) — orrery sizing/orbit model: sun-relative sizes, non-overlapping spacedOrbits pass, fidelity over accuracy.
- [Orrery physical fidelity](orrery-physical-fidelity.md) — rings, tilt, rotation and the moon block. Retrograde falls out of pole geometry so `sign` must ignore obliquity; the texturing frame MUST be right-handed or every planet spins backwards; plus the `lon = atan2` beach-ball trap.
- [System→body drill rush](body-drill-rush.md) — the deeper drill is NOT a dive, so fades and timing can never fix its feel; `OrreryPush` scales the system layer out from the drilled planet. The k² trap: apply the factor exactly once, through `reveal`.
- [Flare playground](flare-playground.md) — animated solar flares at highest LOD; FlarePlayground tuning tool + FlareParams uniform kept in the module until user says remove.
- [Asteroid playground](asteroid-playground.md) — REMOVED 2026-07-28 once the impostor landed (restore from 54e4ff8); keeps the four things worth knowing before rebuilding one — drive the PRODUCTION body shaders not a copy, reproduce the two-pass HDR+tonemap, orreryAlpha must be 1, and camera distance ≥ ~5.1.
- [Nebula playground](nebula-playground.md) — NebulaField (3 styles + star diffusion) wired into the live map; standalone NebulaPlayground tool + NebulaUniforms struct removed 2026-07-20.
- [Planet texturing](planet-texturing.md) — orrery planets procedurally textured: PlanetType/PlanetMaterial → OrreryBodyUniform → orrery_body_fragment; style-index + tag-modifier sync points; atmosphere halo + animated clouds; feel constants now baked as kCloud*/kHalo* in Shaders.metal (OrreryTuning struct + sliders removed).
- [Metal HUD glass hitch](metal-hud-glass-hitch.md) — glass cards (ultraThinMaterial + big shadow) hitch the main-thread MTKView camera fly; drop the material/shadow, keep the animation.
- [Star map hydrate fly hitch](starmap-hydrate-fly-hitch.md) — drill-in fly hitched because a mid-fly hydrate DB write forced a StarSystem blob re-decode on the main thread; fix = defer the hydrate until the transition lands.
- [Star map main-thread load](starmap-main-thread-load.md) — the four costs that jammed other windows + lagged ship icons; round 1 lightening (frame pacing, label throttle, shared OrreryLayout, emission pixel-snapping, SystemDecodeCache) + round 2 (GPU-drawn ship icons replace the SwiftUI overlay; label selection off-main via nonisolated LabelSelection).
- [Metal SPM integration](metal-spm-integration.md) — how to wire a .metal shader + shared C struct header into an SPM library target (process resource, Bundle.module, C target, no defaultIsolation).
- [Civilisations feature](civilisations-feature.md) — Catalog › Civilisations = backend "species" + account reputation blended into one table (nullable totalReputation); cold-load vs reputation-only refresh split; star_regions spec patch.
- [Locations catalog feature](locations-catalog-feature.md) — Catalog › Locations disclosure tree; domain+persistence in UniverseModels, StarsClient/LocationsClient in GameServices (since 2026-07-21), UI in LocationsFeature; blob-per-system persistence, hydrate-on-select.
- [UniverseModels module](locations-catalog-feature.md) — pure models module (no clients since 2026-07-21): census Star/Position/Recon/LifeTier/InventoryItem + the locations domain; wire DTOs internal behind the public LocationDecoding facade.
- [Supervisor-adopts-row flake RESOLVED](supervisor-adopts-row-whole-package-failure.md) — `theSupervisorAdoptsTheRowTheBrainLaunched` is FIXED as of 2026-08-15; the old "deterministic under `--build-system native`, don't attribute it to your change" instruction in the plan docs is stale, and a failure of it is now yours.
- [Brain tick overlap race](brain-tick-overlap-race.md) — `start()`'s brain tick runs CONCURRENTLY with any explicit `tickBrain()`, and both read the pre-launch world. `brainTickCount` increments at entry so it cannot signal completion — a test needing N ticks must drive all N itself.
- [Running package tests](running-package-tests.md) — SPM module tests ARE in the Xcode test plan; schemes with test targets work as expected (`swift test` also works).
- [OpenAPI spec drift leniency](openapi-spec-drift-leniency.md) — POLICY: keep spec STRICT, do NOT relax additionalProperties; patch drift with targeted typed keys.
- [OpenAPI spec layout](openapi-spec-layout.md) — pristine + `-edits` per version, `openapi.json` a symlink to the active one. Carries every upgrade's patch ledger and the two rules that pay off: grep for a sibling schema before hand-typing a patch, and scan every new path for a `default`-only responses block.
- [Undocumented success response throws](undocumented-success-response-throws.md) — an operation declaring only `default` decodes its 200 against the STRICT error schema, so every success throws (quest completion, 2026-07-30).
- [Device command shapes](device-command-shapes.md) — per-command POST response classes (travel=deadline, mine=continuous, scan/census=synchronous, print=enqueued, lifecycle=immediate) + mining lifecycle.
- [Device command taxonomy](device-command-taxonomy.md) — full command inventory + feature gates, live-roster coverage (2026-07-22), unsurfaced commands to fold in (configure/message/replicate/repair; drop detonate), and the approved 9-group grouping + sheet/inline/confirm presentation rule for the DeviceDetailView revamp.
- [controlled_devices is detail-only](controlled-devices-detail-only.md) — `controlled_devices` ships only in GET devices/{code}; the fleet-wide list omits it AND a list sync erases it from `detail`, so read adoption from the drone's `controller_device_code` column (this hid every staged vessel from the Survey Run launcher).
- [Location scope cannot see stowed](location-scope-cannot-see-stowed.md) — `GET devices?location=X` answers PRESENCE and stowing CLEARS location, so a stowed device is out of scope, not stale; in-transit ones ARE returned, which is what misleads. Containment questions must name the device.
- [Staging freshness vs read budget](staging-freshness-vs-read-budget.md) — a mission's WorldSnapshot is local SQLite only, so a `.low` confirm-read deferred by the budget floor made Survey Run preflight read stale silence as "not staged" (and made Retry a structural no-op); fixed by folding stow state out of `device.stowed`/`device.deployed` and by `MissionAction.refreshDevices`.
- [Paged endpoint maxima](paged-endpoint-maxima.md) — always send the max `limit`: 100 everywhere, 50 for `GET devices`; over-asking is silently clamped (not rejected), the spec documents no maxima, and the bare default is 20 — which truncates probes.
- [Location sites endpoint](location-sites-endpoint.md) — GET locations/{designation} polymorphic per location_type; mining sites vs salvage sites, belt inventory, explored-gating; root of the Locations catalog.
- [Feature module import GameSession](feature-module-import-dependencyclients.md) — a new module's domain client needs `import GameSession` (gameClient's home since the 2026-07-21 M2 split; before that GameServices/DependencyClients) for `@Dependency(\.gameClient)`; missing it shows as a misleading key-path inference error.
- [Travel is cheap vs survey](travel-is-cheap-vs-survey.md) — measured: travel ETAs are 1–3 min (max 467s observed) against survey cycles of tens of minutes, so survey automation should optimise COVERAGE, not route length; read durations off `completesAt`, never `lastConfirmedAt`.
- [Operations table retention](operations-table-retention.md) — nothing pruned `operations` until 2026-07-27 and every UI surface fetches it unfiltered; `OperationRetention` now drops terminal ops older than 7d hourly off `DeadlineScheduler.run()` (open ops never pruned, aged on `startedAt` not `lastConfirmedAt`).
- [AMI drones are event-silent](ami-drones-are-event-silent.md) — an adopted drone emits ZERO per-device events, so it is never marked stale and its columns move only when something explicitly reads them. Never wait on one to change by itself; `ami.launched` is the one free launch signal.
- [Survey digest scans](survey-digest-scans.md) — `report.scans[]` is the ONLY channel carrying a Survey Run's per-body intel. The `moons` roster is a SIBLING of `planet`, and a scan is a PARTIAL observation, so it needs `BodyObservation`, never `BodyDetail`.
- [Survey search op shapes](survey-search-op-shapes.md) — survey-drone `search`: `scan` block (eta_seconds, no completes_at) + `scan_complete` relay event; wired as `OperationKind.search`.
- [Travel block leg vs route](travel-block-leg-vs-route.md) — travel block has per-leg (arrives_at/eta_seconds) vs whole-route (final_arrives_at/route_eta_seconds) fields; `device_travel_arrived` completes the trip (per-leg cruise/surge events don't).
- [Travel system-proxy codes](travel-system-proxy-codes.md) — a bare system designation in a travel payload is a PROXY for that system's entry point; the absence of a hyphen is the whole discriminator. One shared `TravelSnapshot.itinerary` rule now feeds all three travel surfaces.
- [SwiftUI View statics trap in tests](swiftui-view-statics-trap-in-tests.md) — pure logic as a static/nested type on a SwiftUI View traps (signal 5) under `swift test`; extract to a plain SwiftUI-free namespace.
- [Device inspector refresh loop](device-inspector-refresh-loop.md) — while-viewing in-place refresh for mining/diverting devices; cadence in `DevicesFeature.refreshDelay`; mining-vs-seeking = pending_* > 0.
- [Replicants feature](replicants-feature.md) — ReplicantsFeature (galaxy directory) + KnownReplicant table (separate from Replicant roster); last-known-location from system_scan replicants block, hooked in CommandClient.
- [Bobnet feature](bobnet-feature.md) — the 3-panel chat, and the rule five successive bugs earned: the pane renders from ONE asynchronously-loaded value, so `isAtLatest`, the divider marker and the scroll `.id` must key on the LOADED channel, never the selection. Carries the `BobnetScrollProbe` recipe.
- [List query in state](list-feature-query-in-state.md) — standard: SQLiteData @FetchAll lives in TCA @ObservableState (not the view), view is a pure renderer; kills empty-state flashes.
- [A @Shared value is not a binding](shared-value-is-not-a-binding.md) — driving a `@Shared` property through `BindingReducer()` compiles and passes but hits Sharing's DEPRECATED setter; write it in a real action via `withLock`. `@Shared` tests are auto-insulated per test.
- [Sidebar feature](sidebar-feature.md) — SidebarFeature owns the left column (header+category list+footer+category selection); MainFeature is now a thin container; progress driven by the Operation table.
- [Account feature](account-feature.md) — AccountFeature module: Profile/Settings/Achievements sheet; consumes achievements + PATCH accounts/me; presented from MainFeature; notifications matrix + email editing deferred.
- [Device refresher dependency](device-refresher-dependency.md) — all device confirm-reads funnel through @Dependency(\.deviceRefresher) over one shared PollCoordinator (in GameServices).
- [Location endpoint presence gate](location-endpoint-presence-gate.md) — CORRECTED 2026-07-27: GET locations/{designation} is gated on EXPLORATION, not presence; the 403 "No replicant in system" message lies, and any explored system rehydrates from anywhere.
- [Census explored is not distance-sorted](census-explored-is-not-distance-sorted.md) — the census sorts by CURRENT distance while `explored` is HISTORY, so explored systems scatter across all 141 pages (SOL was on p13); never stop the overlay walk early, and GET /v1/locations is the cheap partial repair.
- [Location Events feature](location-events-feature.md) — Missions/quest-log; accounts/events is the account-wide list w/ full detail; LocationEvent table in GameModels; Event Log renamed Operations Log.
- [Experience + completion events](experience-and-completion-events.md) — a quest's `rewards.xp` is DOUBLE-delivered, so `event.completed` is display-only and XP credits on stream provenance only. `accounts/events` NEVER returns `consumed`, so `merging` must carry a captured manifest across.
- [GameDatabase module](gamedatabase-module.md) — single schema-composition point; previews/tests call `GameDatabase.bootstrap()` instead of hand-rolling migrators.
- [Erase-on-schema-change](erase-on-schema-change.md) — why adding a table used to wipe the DB (GRDB compares against the last APPLIED identifier, so mid-list migrations erased); replaced 2026-07-26 by the append-only `GameDatabase.manifest` + `DatabaseReset`.
- [Locations list flatten perf](locations-list-flatten-perf.md) — big macOS hierarchical list: flatten to visible rows + flat List beats children:/DisclosureGroup (generic-metadata/ARC storm at 5,770 nodes).
- [WorldSnapshot.read cost](snapshot-read-cost.md) — what actually dominates a directive's per-tick read: a scope naming a GROWING set (303 targets of blobs) bounds nothing; carries the measured table and the two terms still unbounded.
- [Dispatched operations, two-set union](dispatched-operations-two-set-union.md) — `dispatchedOperations` unions a kind-filtered mission set (`print`/`travel` only) with a kind-agnostic audit set, because filtering the audit half would silently stop `recordCompletedOps` closing `launch`/`recall`/`deploy`; names which consumer reads which kind.
- [Fast ISO-8601 date columns](fast-iso8601-parsing.md) — `@Column(as: Date.FastISO8601Representation.self)` decodes by hand, binds through the library so no migration; debug-vs-release inversion, the 2001 reference date, and Foundation's pre-1582 Julian hybrid.
- [JSON projection beats blob decode](json-projection-over-blob-decode.md) — `beltsBySystem` asks SQLite via `json_each` instead of decoding every blob: 73.9ms → 13.7ms. `json_each` raises on invalid JSON and kills the WHOLE query, so the ARGUMENT must be sanitized. JSONB alone would be slower.
- [Locations forest inventory index](locations-forest-inventory-index.md) — the forest rebuild was O(stars × footprints) ≈ 107M string compares; always quadratic, and the survey automation made it bite. Fetch 1.20s → 0.12s. A read-time SQL projection was measured and REJECTED here, reversing what it bought for belts.
- [SQLite DB location](sqlite-db-location.md) — path to the running app DB for direct sqlite3 inspection; census `stars` lags `systemDetails`
- [Location response schema drift](location-response-schema-drift.md) — openapi.json patch so GET locations/{designation} decodes (asteroid_belt); re-apply after re-fetch
- [Decode diagnostics decorator](decode-diagnostics-decorator.md) — OpenAPI decode errors logged centrally via DiagnosticAPIClient (APIProtocol decorator); regen with scripts/gen-diagnostic-client.py; call sites stay clean
- [SPM stale layout crash](spm-stale-layout-crash.md) — RETESTED 2026-07-25: does NOT reproduce under the default swiftbuild engine; the rm -rf Modules/.build ritual is obsolete for swift test (was a deprecated-llbuild artifact). Xcode app-target half still stands.
- [API drift backlog](api-drift-backlog.md) — no known open drifts as of 2026-07-20; swift test fully green (records the resolved new_resource/terminating/adopt-read fixes).
- [Preview sheets need .sheet(item:)](preview-sheet-item-not-ispresented.md) — travel/print preview sheets must use .sheet(item:) not isPresented, else rapid back-to-back presentations drop the dialog; fixed in 3 travel entry points + effect cancellation.
- [SourceKit-LSP index reality](sourcekit-lsp-warmup.md) — FIXED: `.sourcekit-lsp/config.json` points LSP at SwiftPM's swiftbuild BSP server so it reads the index your own builds write; needs `link-index-store.sh` once per checkout. The index is only as fresh as your last build.
- [Loop legs must dispatch in-step](loop-legs-must-dispatch-in-step.md) — a step looping over a fleet must pass `confirmStep: nil`: a dispatch is a `return`, so a forward confirm step ends the loop on the first subject and strands the rest. `EventRun.departing` ordered one freighter of two and stalled `.vesselPositionUnconfirmed` for ever.
- [A convoy's legs fly abreast](convoy-legs-fly-abreast.md) — a multi-hull step must step PAST a hull whose travel op is open, never return its `.wait`: returning it costs one full crossing per hull. Every `departing` fixture teleported hulls and opened no operation, so the suite could not see it.
- [The event convoy has no singular form](event-convoy-has-no-singular.md) — `freighterCodes` is the only lease and `Convoy` exposes no lead hull; load and sweep share one divider differing only in room measure and unplaced policy; the `freighterCode` column is DROPPED, and its migration must stay after `addFreighterCodes`. Closed the `depositing` bound that shrank as hulls emptied and left the second hold full.
- [Stall triage 2026-08-19](stall-triage-2026-08-19.md) — four live stall causes, none a Stage 0-2 regression: EventRun re-decided on an EMPTY catalogue, an `ftl_relay` names its carrier under `stowed_in`, a co-tenant print completion closed the wrong op (23 of 235), and a one-freighter convoy could not carry an 800-unit payload. **Amended 2026-08-19 pm**: the third fix went into the close production does NOT call (`GameSync` routes events through `applyDeviceEvent`, not `applyOperationEvent`), and the misattribution was never the whole stall — the flat `PrintJob.deadline` cannot see a shared bench's queue, which runs deeper than 30 minutes.
